---
phase: 04-panorama-e-inteligencia
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/components/panorama/workspace-shell.tsx
  - src/components/panorama/overview-strip.tsx
  - src/components/panorama/signal-list.tsx
  - src/components/panorama/intelligence-readiness.tsx
  - src/components/panorama/milestone-direction.tsx
  - src/test/panorama-workspace.test.tsx
autonomous: true
requirements:
  - DASH-03
must_haves:
  truths:
    - "Usuario ve panorama consolidado com modulos, sinais, readiness e opcoes de milestone."
    - "Refresh do panorama usa apenas `/api/panorama`."
    - "A UI comunica preparo para inteligencia/integracoes sem produto spoofing."
  artifacts:
    - path: "src/components/panorama/workspace-shell.tsx"
      provides: "Experiencia principal do workspace de panorama."
    - path: "src/components/panorama/overview-strip.tsx"
      provides: "Resumo visual dos tres modulos operacionais."
    - path: "src/components/panorama/signal-list.tsx"
      provides: "Lista limitada e acionavel de sinais cross-domain."
    - path: "src/components/panorama/intelligence-readiness.tsx"
      provides: "Preparo para inteligencia/integracoes sem prometer execucao real."
    - path: "src/components/panorama/milestone-direction.tsx"
      provides: "Base visual para decidir proxima milestone."
    - path: "src/test/panorama-workspace.test.tsx"
      provides: "Testes RTL da experiencia de panorama."
  key_links:
    - from: "src/components/panorama/workspace-shell.tsx"
      to: "/api/panorama"
      via: "refreshWorkspace"
      pattern: "fetch\\(\"/api/panorama\""
---

<objective>
Construir a UI final do panorama dentro do workspace `/panorama`.

Purpose: transformar o aggregate de DASH-03 em leitura operacional usavel, com componentes densos, refresh read-only e base visual para decisao de milestone.
Output: componentes de panorama, shell de workspace e testes RTL da experiencia principal.
</objective>

<execution_context>
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-panorama-e-inteligencia/04-RESEARCH.md
@.planning/phases/04-panorama-e-inteligencia/04-PATTERNS.md
@.planning/phases/04-panorama-e-inteligencia/04-panorama-e-inteligencia-02-SUMMARY.md
@src/components/dashboard/status-block.tsx
@src/components/dashboard/ops-radar.tsx
@src/components/dashboard/execution-radar.tsx
@src/components/dashboard/agenda-radar.tsx
@src/components/panorama/workspace-shell.tsx
@src/types/panorama.ts
@src/test/agenda-workspace.test.tsx

<interfaces>
Componentes existentes a reutilizar:
```tsx
export function StatusBlock({ emphasis = "default", label, value }: StatusBlockProps) {
  return (
    <article className={`status-block status-block--${emphasis}`}>
      <p>{label}</p>
      <strong>{value}</strong>
    </article>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar componentes finais de panorama</name>
  <read_first>
    - `src/components/dashboard/status-block.tsx`
    - `src/components/dashboard/ops-radar.tsx`
    - `src/components/dashboard/execution-radar.tsx`
    - `src/components/dashboard/agenda-radar.tsx`
    - `src/types/panorama.ts`
  </read_first>
  <files>src/components/panorama/overview-strip.tsx, src/components/panorama/signal-list.tsx, src/components/panorama/intelligence-readiness.tsx, src/components/panorama/milestone-direction.tsx, src/test/panorama-workspace.test.tsx</files>
  <action>Crie componentes pequenos e tipados para `OperationalPanorama`: `OverviewStrip` usa `StatusBlock` para mostrar total e pressao por modulo; `SignalList` mostra sinais ordenados por severidade com links para `targetRoute`; `IntelligenceReadiness` separa dados disponiveis e ausentes para inteligencia/integracoes como preparo; `MilestoneDirection` lista opcoes de proxima milestone com evidencias. Escreva testes RTL que renderizam esses componentes diretamente, sem passar por `WorkspaceShell`, e verificam textos de modulos, sinais, readiness, opcoes e estado vazio. Use copy em Portugues, labels curtas e nao mencione conectores reais como ativos.</action>
  <verify>
    <automated>npm run test -- --run src/test/panorama-workspace.test.tsx</automated>
    <automated>rg "OverviewStrip|SignalList|IntelligenceReadiness|MilestoneDirection" src/components/panorama src/test/panorama-workspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Componentes recebem props tipadas de `src/types/panorama.ts` e nao usam `any`.
    - `SignalList` limita renderizacao ao array ja limitado pelo dominio e nao faz fetch.
    - Testes diretos dos componentes comprovam modulo `Inbox`, `Tarefas`, `Agenda`, pelo menos um sinal e uma opcao de milestone.
    - Copy de readiness usa termos como `preparo`, `base` ou `sinais`, nao `conectado`, `sincronizado` ou `agente ativo`.
  </acceptance_criteria>
  <done>Componentes de panorama renderizam overview, sinais, readiness e direcao de milestone com testes RTL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar shell de panorama com refresh read-only</name>
  <read_first>
    - `src/components/panorama/workspace-shell.tsx`
    - `src/test/panorama-workspace.test.tsx`
  </read_first>
  <files>src/components/panorama/workspace-shell.tsx, src/test/panorama-workspace.test.tsx</files>
  <action>Substitua o shell inicial por um workspace completo sem editar CSS neste plano: header com estado operacional, refresh read-only usando `fetch("/api/panorama")`, grid principal com `OverviewStrip`, `SignalList`, `IntelligenceReadiness` e `MilestoneDirection`, e tratamento de erro curto. Mova para esta tarefa as assercoes RTL que renderizam a experiencia por meio de `WorkspaceShell`, incluindo textos de modulos, sinais, readiness, opcoes, refresh bem-sucedido e erro de refresh. A sidebar e os ajustes globais de navegacao/estilo ficam no plano 04-04 para manter o escopo abaixo do limite do checker. Nao crie cards aninhados, nao adicione landing page dentro do workspace e mantenha dimensoes estaveis para blocos de status/listas usando as classes existentes ate o ajuste CSS do plano seguinte.</action>
  <verify>
    <automated>npm run test -- --run src/test/panorama-workspace.test.tsx</automated>
    <automated>rg "fetch\\(\"/api/panorama\"|workspace-grid|OverviewStrip|SignalList|IntelligenceReadiness|MilestoneDirection" src/components/panorama/workspace-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Refresh do shell chama somente `/api/panorama`.
    - Shell nao chama `/api/inbox`, `/api/tasks` ou `/api/agenda`.
    - Testes renderizam `WorkspaceShell` e cobrem modulos, sinais, readiness, opcoes de milestone, refresh bem-sucedido e erro de refresh.
    - A experiencia principal de panorama renderiza sem depender de dados estaticos ou fixtures.
  </acceptance_criteria>
  <done>Workspace de panorama integra componentes finais e refresh read-only com testes RTL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/panorama` -> browser | payload agregado passa a ser renderizado e atualizado no cliente |
| readiness -> usuario | textos podem ser confundidos com capacidade ativa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | I | componentes de panorama | mitigate | renderizar somente `OperationalPanorama`; nao buscar nem exibir entidades cruas |
| T-04-03-02 | D | `WorkspaceShell.refreshWorkspace` | mitigate | uma chamada para `/api/panorama`, estados de erro e sem polling automatico |
| T-04-03-03 | S | copy de readiness | mitigate | usar linguagem de preparo/evidencia; testes rejeitam termos que indicam integracao ou agente ativo |
</threat_model>

<verification>
Focused:
- `npm run test -- --run src/test/panorama-workspace.test.tsx`

Full:
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
</verification>

<success_criteria>
- Usuario consegue abrir `/panorama` e ver modulos, sinais, readiness e opcoes de milestone derivados do aggregate.
- Refresh do panorama usa somente `/api/panorama`.
- A UI comunica preparo para inteligencia/integracoes sem produto spoofing.
- Workspaces existentes ainda nao sao alterados por este plano; navegacao compartilhada entra no plano 04-04.
</success_criteria>

<source_coverage_audit>
GOAL: leitura operacional ampla vira experiencia navegavel e acionavel dentro de `/panorama`.
REQ: DASH-03 coberto por UI que consome `OperationalPanorama`.
RESEARCH: padroes de workspace, status blocks, sinais e readiness aplicados.
CONTEXT: limites de nao implementar IA/integracoes reais preservados.
</source_coverage_audit>

<output>
After completion, create `.planning/phases/04-panorama-e-inteligencia/04-panorama-e-inteligencia-03-SUMMARY.md`
</output>
