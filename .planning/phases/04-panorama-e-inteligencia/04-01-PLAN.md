---
phase: 04-panorama-e-inteligencia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/panorama.ts
  - src/lib/intelligence/context-packet.ts
  - src/db/queries/panorama.ts
  - src/test/panorama-domain.test.ts
autonomous: true
requirements:
  - DASH-03
must_haves:
  truths:
    - "O panorama operacional combina estados reais de inbox, tarefas e agenda."
    - "Sinais cross-domain sao derivados no servidor a partir dos resumos existentes."
    - "Readiness de inteligencia/integracoes aparece como preparo tipado, sem chamada externa."
    - "A fase nao cria store persistido proprio para panorama."
  artifacts:
    - path: "src/types/panorama.ts"
      provides: "Contratos de modulos, sinais, readiness, context packet e opcoes de milestone."
    - path: "src/lib/intelligence/context-packet.ts"
      provides: "Payload resumido e deterministico para uso posterior por IA, sem dados crus."
    - path: "src/db/queries/panorama.ts"
      provides: "Aggregate owner cross-domain para leitura operacional ampla."
    - path: "src/test/panorama-domain.test.ts"
      provides: "Cobertura de dominio com stores reais de inbox, tasks e agenda."
  key_links:
    - from: "src/db/queries/panorama.ts"
      to: "src/db/queries/inbox.ts"
      via: "getDashboardSummary"
      pattern: "getDashboardSummary"
    - from: "src/db/queries/panorama.ts"
      to: "src/db/queries/tasks.ts"
      via: "getExecutionSummary"
      pattern: "getExecutionSummary"
    - from: "src/db/queries/panorama.ts"
      to: "src/db/queries/agenda.ts"
      via: "getAgendaSummary"
      pattern: "getAgendaSummary"
---

<objective>
Criar o contrato e o aggregate owner de panorama para cumprir DASH-03 com leitura real dos dominios existentes.

Purpose: consolidar a visao operacional ampla sem inflar `DashboardSummary` de inbox e sem criar persistencia nova para panorama.
Output: tipos compartilhados, context packet resumido, query `getOperationalPanorama()` e testes de dominio.
</objective>

<execution_context>
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-panorama-e-inteligencia/04-RESEARCH.md
@.planning/phases/04-panorama-e-inteligencia/04-PATTERNS.md
@.planning/phases/03-coordenacao-de-agenda/03-VERIFICATION.md
@src/types/inbox.ts
@src/types/tasks.ts
@src/types/agenda.ts
@src/db/queries/inbox.ts
@src/db/queries/tasks.ts
@src/db/queries/agenda.ts

<interfaces>
Contratos existentes relevantes:
```ts
export interface DashboardSummary {
  byStatus: Array<{ status: InboxStatus; count: number }>;
  highPriorityCount: number;
  waitingOnResponseCount: number;
  unreviewedTriageCount: number;
  oldestItems: Array<{ id: number; title: string; ageHours: number }>;
  averageResponseAgeHours: number;
}

export interface TaskSummary {
  totalCount: number;
  activeCount: number;
  readyToConvertCount: number;
  unassignedCount: number;
  blockedCount: number;
  criticalCount: number;
  agedCount: number;
}

export interface AgendaSummary {
  totalCount: number;
  activeCount: number;
  overdueCount: number;
  dueTodayCount: number;
  upcomingCount: number;
  futureCount: number;
  completedCount: number;
  cancelledCount: number;
  criticalItems: Array<{ id: number; title: string; dueAt: string; urgencyState: AgendaUrgencyState }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir contratos de panorama e context packet</name>
  <read_first>
    - `.planning/phases/04-panorama-e-inteligencia/04-RESEARCH.md`
    - `.planning/phases/04-panorama-e-inteligencia/04-PATTERNS.md`
    - `src/types/inbox.ts`
    - `src/types/tasks.ts`
    - `src/types/agenda.ts`
  </read_first>
  <files>src/types/panorama.ts, src/lib/intelligence/context-packet.ts, src/test/panorama-domain.test.ts</files>
  <action>Crie `src/types/panorama.ts` com unions constantes para `PANORAMA_MODULES`, severidade de sinal e direcao de milestone. Inclua `PanoramaModuleState`, `PanoramaSignal`, `IntelligenceReadinessSnapshot`, `IntegrationReadinessSnapshot`, `NextMilestoneOption`, `PanoramaContextPacket` e `OperationalPanorama`. Os modulos devem ser exatamente `inbox`, `execution` e `agenda`, com rotas `/inbox`, `/execucao` e `/agenda`. Crie `src/lib/intelligence/context-packet.ts` com `buildPanoramaContextPacket(panorama: OperationalPanorama): PanoramaContextPacket`, retornando somente contadores, sinais resumidos, prontidao e opcoes de milestone. Nao inclua `descriptionRaw`, listas completas de entidades, prompts, chamadas OpenAI, OAuth, conectores ou campos que simulem integracao real.</action>
  <verify>
    <automated>rg "PANORAMA_MODULES|PanoramaModuleState|PanoramaSignal|IntelligenceReadinessSnapshot|IntegrationReadinessSnapshot|NextMilestoneOption|OperationalPanorama|PanoramaContextPacket" src/types/panorama.ts</automated>
    <automated>rg "buildPanoramaContextPacket|PanoramaContextPacket" src/lib/intelligence/context-packet.ts</automated>
    <automated>if (rg "descriptionRaw|OpenAI|OAuth|WhatsApp|Instagram|Google Calendar|Outlook" src/lib/intelligence/context-packet.ts) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/panorama.ts` exporta contratos usados por API, UI e testes sem importar codigo de componentes.
    - `src/lib/intelligence/context-packet.ts` expoe um builder deterministico e nao importa `openai`.
    - `rg "descriptionRaw|rawStore|readFile|writeFile|openai" src/types/panorama.ts src/lib/intelligence/context-packet.ts` nao encontra exposicao de dados crus nem chamada de IA.
  </acceptance_criteria>
  <done>Contratos e context packet de panorama existem, sao tipados e nao expõem dados crus nem capacidades externas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar `getOperationalPanorama` com stores reais</name>
  <read_first>
    - `src/db/queries/inbox.ts`
    - `src/db/queries/tasks.ts`
    - `src/db/queries/agenda.ts`
    - `src/test/agenda-domain.test.ts`
    - `src/test/dashboard-review.test.ts`
  </read_first>
  <files>src/db/queries/panorama.ts, src/test/panorama-domain.test.ts</files>
  <action>Escreva primeiro testes em `src/test/panorama-domain.test.ts` que resetam inbox, tasks e agenda, semeiam entradas revisadas, tarefas bloqueadas/sem responsavel e agenda vencida/proxima, e chamam `getOperationalPanorama()`. Implemente `src/db/queries/panorama.ts` como aggregate owner read-only: use `Promise.all([getDashboardSummary(), getExecutionSummary(), getAgendaSummary()])`, derive `modules`, `signals`, `intelligenceReadiness`, `integrationReadiness`, `contextPacket` e `nextMilestoneOptions`. Gere sinais acionaveis como `inbox-waiting-response`, `inbox-unreviewed-triage`, `execution-blocked`, `execution-unassigned`, `agenda-overdue` e `agenda-upcoming` apenas quando os contadores correspondentes forem maiores que zero. Limite a lista de sinais a no maximo 8 itens por severidade/pressao e nunca crie `.data/panorama*.json` ou schema de panorama.</action>
  <verify>
    <automated>npm run test -- --run src/test/panorama-domain.test.ts</automated>
    <automated>rg "getDashboardSummary|getExecutionSummary|getAgendaSummary|Promise.all|buildPanoramaContextPacket" src/db/queries/panorama.ts</automated>
    <automated>if (rg "mkdir|readFile|writeFile|panorama-items|OpenAI|OAuth" src/db/queries/panorama.ts) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Testes provam que o panorama muda quando stores reais de inbox, tarefas e agenda mudam.
    - `src/db/queries/panorama.ts` importa os tres resumos existentes e nao importa APIs de filesystem.
    - `getOperationalPanorama()` retorna um objeto `OperationalPanorama` com tres modulos, sinais limitados, readiness e opcoes de milestone.
    - Nenhum teste ou query cria fixture hardcoded como substituto dos stores reais.
  </acceptance_criteria>
  <done>`getOperationalPanorama()` entrega snapshot cross-domain real, limitado e coberto por teste de dominio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stores de dominio -> aggregate panorama | dados de inbox, tasks e agenda cruzam fronteira para um contrato unico |
| panorama -> context packet | resumo de operacao pode virar entrada para IA em milestone posterior |
| aggregate -> cliente/API | payload read-only pode crescer e expor mais dados que o necessario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I | `buildPanoramaContextPacket` | mitigate | incluir somente contadores, ids de sinais e textos resumidos; excluir `descriptionRaw` e stores completos |
| T-04-01-02 | D | `getOperationalPanorama` | mitigate | limitar sinais a no maximo 8 e consumir resumos existentes em vez de expor listas completas |
| T-04-01-03 | S | readiness de IA/integracoes | mitigate | modelar campos como `readiness`/`missingSignals`, sem status que sugira conector ou agente ativo |
| T-04-01-04 | T | aggregate cross-domain | mitigate | derivar estado por `Promise.all` dos resumos canonicos, sem aceitar input externo nesta query |
</threat_model>

<verification>
Focused:
- `npm run test -- --run src/test/panorama-domain.test.ts`
- `rg "getDashboardSummary|getExecutionSummary|getAgendaSummary|OperationalPanorama" src/db/queries/panorama.ts src/types/panorama.ts`

Full:
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
</verification>

<success_criteria>
- DASH-03 tem um aggregate owner proprio em `src/db/queries/panorama.ts`.
- O panorama reflete inbox, tarefas e agenda a partir de queries reais.
- Context packet e readiness preparam expansao sem implementar conectores, OAuth, agentes ou novas chamadas de IA.
- A fase mantem persistencia runtime file-backed apenas nos dominios existentes.
</success_criteria>

<source_coverage_audit>
GOAL: Fase 4 exige leitura operacional ampla e preparo para IA/integracoes; coberto por contratos, aggregate e context packet.
REQ: DASH-03 coberto diretamente por `getOperationalPanorama()` e testes de stores reais.
RESEARCH: recomendacao de `src/db/queries/panorama.ts` como owner cross-domain coberta; out-of-scope de integracoes e IA respeitado.
CONTEXT: nao ha `04-CONTEXT.md`; restricoes fornecidas no pedido foram incorporadas.
</source_coverage_audit>

<output>
After completion, create `.planning/phases/04-panorama-e-inteligencia/04-panorama-e-inteligencia-01-SUMMARY.md`
</output>
