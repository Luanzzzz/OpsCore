---
phase: 04-panorama-e-inteligencia
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/api/panorama/route.ts
  - src/app/(workspace)/panorama/page.tsx
  - src/app/(workspace)/panorama/loading.tsx
  - src/app/(workspace)/panorama/error.tsx
  - src/components/panorama/workspace-shell.tsx
  - src/test/panorama-api.test.ts
autonomous: true
requirements:
  - DASH-03
must_haves:
  truths:
    - "GET /api/panorama retorna o contrato real criado no dominio."
    - "`/panorama` hidrata no servidor sem self-fetch e sem fixtures."
    - "A rota de workspace tem estados de loading e erro coerentes com os demais workspaces."
  artifacts:
    - path: "src/app/api/panorama/route.ts"
      provides: "Endpoint read-only de panorama operacional."
      exports: ["GET"]
    - path: "src/app/(workspace)/panorama/page.tsx"
      provides: "Rota server-first para o workspace de panorama."
    - path: "src/components/panorama/workspace-shell.tsx"
      provides: "Shell inicial tipado que recebe `OperationalPanorama`."
    - path: "src/test/panorama-api.test.ts"
      provides: "Cobertura HTTP do contrato de panorama."
  key_links:
    - from: "src/app/api/panorama/route.ts"
      to: "src/db/queries/panorama.ts"
      via: "GET chama `getOperationalPanorama`"
      pattern: "getOperationalPanorama"
    - from: "src/app/(workspace)/panorama/page.tsx"
      to: "src/components/panorama/workspace-shell.tsx"
      via: "prop `initialPanorama`"
      pattern: "initialPanorama"
---

<objective>
Expor o panorama por API read-only e por uma rota `/panorama` server-first.

Purpose: tornar o aggregate de DASH-03 consumivel por cliente, testes e workspace sem fan-out para `/api/inbox`, `/api/tasks` ou `/api/agenda`.
Output: route handler, rota App Router, loading/error states, shell inicial tipado e testes de API.
</objective>

<execution_context>
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projetos_pessoais/OpsCore/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-panorama-e-inteligencia/04-RESEARCH.md
@.planning/phases/04-panorama-e-inteligencia/04-PATTERNS.md
@.planning/phases/04-panorama-e-inteligencia/04-panorama-e-inteligencia-01-SUMMARY.md
@src/db/queries/panorama.ts
@src/types/panorama.ts
@src/app/api/agenda/route.ts
@src/app/(workspace)/agenda/page.tsx
@src/app/(workspace)/agenda/loading.tsx
@src/app/(workspace)/agenda/error.tsx

<interfaces>
Contrato criado no plano anterior:
```ts
export interface OperationalPanorama {
  modules: PanoramaModuleState[];
  signals: PanoramaSignal[];
  intelligenceReadiness: IntelligenceReadinessSnapshot;
  integrationReadiness: IntegrationReadinessSnapshot;
  contextPacket: PanoramaContextPacket;
  nextMilestoneOptions: NextMilestoneOption[];
}

export async function getOperationalPanorama(): Promise<OperationalPanorama>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar `GET /api/panorama` com contrato read-only</name>
  <read_first>
    - `src/app/api/tasks/route.ts`
    - `src/app/api/agenda/route.ts`
    - `src/app/api/dashboard/route.ts`
    - `src/db/queries/panorama.ts`
    - `src/test/agenda-api.test.ts`
  </read_first>
  <files>src/app/api/panorama/route.ts, src/test/panorama-api.test.ts</files>
  <action>Crie `src/app/api/panorama/route.ts` com `GET` que chama `getOperationalPanorama()` e retorna `NextResponse.json(panorama)`. Nao aceite query params nesta entrega; se algum parametro for lido por necessidade tecnica, valide explicitamente e retorne `400` em input invalido. Em `src/test/panorama-api.test.ts`, use `NextRequest`, resete os tres stores, semeie dados reais por helpers de inbox/tasks/agenda, chame `GET` e verifique status `200`, modulos `inbox`/`execution`/`agenda`, sinais derivados e ausencia de campos crus como `descriptionRaw` no JSON serializado.</action>
  <verify>
    <automated>npm run test -- --run src/test/panorama-domain.test.ts src/test/panorama-api.test.ts</automated>
    <automated>rg "export async function GET|getOperationalPanorama|NextResponse.json" src/app/api/panorama/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/panorama` retorna diretamente `OperationalPanorama`.
    - Teste de API prova que o response muda com estados reais de inbox, tarefas e agenda.
    - O teste falha se o payload serializado contiver `descriptionRaw`.
    - A rota nao importa `openai`, `fs`, conectores externos ou endpoints de calendario.
  </acceptance_criteria>
  <done>Endpoint read-only de panorama retorna o contrato real e passa cobertura API focada.</done>
</task>

<task type="auto">
  <name>Task 2: Criar rota server-first `/panorama` e estados de rota</name>
  <read_first>
    - `src/app/(workspace)/agenda/page.tsx`
    - `src/app/(workspace)/agenda/loading.tsx`
    - `src/app/(workspace)/agenda/error.tsx`
    - `src/components/agenda/workspace-shell.tsx`
    - `src/types/panorama.ts`
  </read_first>
  <files>src/app/(workspace)/panorama/page.tsx, src/app/(workspace)/panorama/loading.tsx, src/app/(workspace)/panorama/error.tsx, src/components/panorama/workspace-shell.tsx</files>
  <action>Crie `src/app/(workspace)/panorama/page.tsx` como Server Component que chama `getOperationalPanorama()` diretamente e passa `initialPanorama` para `WorkspaceShell`. Crie `loading.tsx` e `error.tsx` copiando a estrutura dos workspaces existentes, com copy de panorama em Portugues e sem sugerir falha de integracao externa ou IA. Crie `src/components/panorama/workspace-shell.tsx` como shell client inicial tipado, recebendo `initialPanorama`, renderizando titulo, os tres modulos, uma lista simples de sinais e um botao de refresh que busca apenas `/api/panorama`. Esta shell deve compilar e ser ampliada no plano de UI, nao deve chamar `/api/inbox`, `/api/tasks` ou `/api/agenda` separadamente.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>rg "getOperationalPanorama|initialPanorama|WorkspaceShell" "src/app/(workspace)/panorama/page.tsx" src/components/panorama/workspace-shell.tsx</automated>
    <automated>rg "\"/api/panorama\"" src/components/panorama/workspace-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/panorama/page.tsx` nao usa `fetch` nem self-fetch para carregar dados iniciais.
    - `WorkspaceShell` compila com `OperationalPanorama` e nao usa `any`.
    - Loading e error states existem e usam classes compartilhadas `workspace-loading` e `workspace-error-panel`.
    - `rg "/api/(inbox|tasks|agenda)" src/components/panorama/workspace-shell.tsx` nao encontra fan-out de cliente.
  </acceptance_criteria>
  <done>`/panorama` existe como rota server-first compilavel com loading, error e shell inicial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente -> `/api/panorama` | requisicoes podem carregar query params inesperados |
| `/api/panorama` -> cliente | response pode expor dados agregados amplos |
| `/panorama` server route -> client shell | dados server-side viram estado interativo no browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | `src/app/api/panorama/route.ts` | mitigate | nao aceitar filtros nesta entrega; se params forem lidos, validar e retornar `400` |
| T-04-02-02 | I | API response | mitigate | retornar o aggregate resumido; teste deve rejeitar `descriptionRaw` no JSON |
| T-04-02-03 | D | `WorkspaceShell.refreshWorkspace` | mitigate | buscar somente `/api/panorama`, evitando multiplas chamadas e payloads brutos |
| T-04-02-04 | S | error/loading copy | mitigate | copy deve falar de leitura operacional indisponivel, sem dizer que IA ou integracoes reais estao ativas |
</threat_model>

<verification>
Focused:
- `npm run test -- --run src/test/panorama-domain.test.ts src/test/panorama-api.test.ts`
- `npm run build`

Full:
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
</verification>

<success_criteria>
- `GET /api/panorama` entrega panorama real e read-only.
- `/panorama` hidrata a partir do servidor com `getOperationalPanorama()`.
- A tela inicial de panorama pode ser acessada sem quebrar build antes dos componentes finais.
- Nenhum endpoint ou componente novo implementa OAuth, calendario externo, canal externo, agente autonomo ou nova chamada de IA.
</success_criteria>

<source_coverage_audit>
GOAL: leitura operacional ampla passa a ter API e rota navegavel.
REQ: DASH-03 coberto por endpoint e page server-first consumindo `OperationalPanorama`.
RESEARCH: padrao App Router/API dos workspaces existentes aplicado; fan-out e payload cru evitados.
CONTEXT: restricao de nao criar integracoes reais mantida por rota read-only sem query complexa.
</source_coverage_audit>

<output>
After completion, create `.planning/phases/04-panorama-e-inteligencia/04-panorama-e-inteligencia-02-SUMMARY.md`
</output>
