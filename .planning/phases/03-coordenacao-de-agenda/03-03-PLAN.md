---
phase: 03-coordenacao-de-agenda
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/(workspace)/agenda/page.tsx
  - src/app/(workspace)/agenda/loading.tsx
  - src/app/(workspace)/agenda/error.tsx
  - src/components/agenda/workspace-shell.tsx
  - src/components/agenda/agenda-table.tsx
  - src/components/agenda/filters-bar.tsx
  - src/components/detail/agenda-detail-panel.tsx
  - src/components/dashboard/agenda-radar.tsx
  - src/components/inbox/workspace-shell.tsx
  - src/components/execution/workspace-shell.tsx
  - src/app/globals.css
autonomous: true
requirements_addressed:
  - AGND-02
  - AGND-03
must_haves:
  truths:
    - "`/agenda` e a primeira tela real da agenda, sem landing page."
    - "Lista mostra tipo, titulo, origem, responsavel, vencimento e estado temporal sem abrir detalhe."
    - "Detalhe mostra snapshot da origem, notas, status e acoes de concluir/reagendar/cancelar."
    - "Radar deixa vencidos, hoje e proximos evidentes."
    - "Sidebar compartilhada mostra Inbox, Tarefas e Agenda."
  artifacts:
    - path: "src/app/(workspace)/agenda/page.tsx"
      provides: "Entrada server-first da agenda."
    - path: "src/components/agenda/workspace-shell.tsx"
      provides: "Shell client com refresh, selecao, filtros e mutacoes."
    - path: "src/components/agenda/agenda-table.tsx"
      provides: "Lista densa de agenda."
    - path: "src/components/detail/agenda-detail-panel.tsx"
      provides: "Painel de detalhe e acoes."
    - path: "src/components/dashboard/agenda-radar.tsx"
      provides: "Resumo temporal compacto."
  key_links:
    - from: "src/app/(workspace)/agenda/page.tsx"
      to: "src/db/queries/agenda.ts"
      via: "Leitura server-first"
      pattern: "getAgendaItems|getAgendaItemById|getAgendaSummary"
    - from: "src/components/agenda/workspace-shell.tsx"
      to: "/api/agenda"
      via: "Refresh apos filtros e mutacoes"
      pattern: "fetch\\(\"/api/agenda"
    - from: "src/components/detail/agenda-detail-panel.tsx"
      to: "/api/agenda/[id]/status"
      via: "Concluir/cancelar"
      pattern: "onUpdateStatus"
---

<objective>
Construir a superficie principal `/agenda` como lista operacional densa com detalhe lateral e radar temporal.

Purpose: entregar AGND-02 e AGND-03 como fluxo unico de vencimentos, compromissos e follow-ups, preservando a linguagem visual da fase 2.
Output: rota server-first, shell client, tabela, filtros, detalhe, radar, estilos e navegacao compartilhada.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coordenacao-de-agenda/03-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@src/app/(workspace)/execucao/page.tsx
@src/components/execution/workspace-shell.tsx
@src/components/execution/tasks-table.tsx
@src/components/execution/filters-bar.tsx
@src/components/detail/task-detail-panel.tsx
@src/components/dashboard/execution-radar.tsx
@src/app/globals.css

<interfaces>
Contratos de agenda criados no plano 03-01:
```ts
type AgendaWorkspaceProps = {
  initialItems: AgendaListItem[];
  initialSelectedItem: AgendaDetail | null;
  initialSummary: AgendaSummary;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar rota `/agenda`, shell client e navegacao compartilhada</name>
  <files>src/app/(workspace)/agenda/page.tsx, src/app/(workspace)/agenda/loading.tsx, src/app/(workspace)/agenda/error.tsx, src/components/agenda/workspace-shell.tsx, src/components/inbox/workspace-shell.tsx, src/components/execution/workspace-shell.tsx</files>
  <action>Crie `src/app/(workspace)/agenda/page.tsx` no mesmo padrao server-first de `/execucao`: carregar `items`, `selectedItem` e `summary` via `getAgendaItems`, `getAgendaItemById` e `getAgendaSummary`, usando o primeiro item como selecao inicial quando existir. Crie loading/error com copy de agenda operacional. Crie `src/components/agenda/workspace-shell.tsx` como Client Component com estado para `items`, `summary`, `selectedId`, `selectedItem`, `filters`, `notice`, `errorMessage` e `useTransition`. O shell deve buscar detalhe em `/api/agenda/[id]`, refresh em `/api/agenda`, reagendar por PATCH `/api/agenda/[id]` e concluir/cancelar por POST `/api/agenda/[id]/status`. Atualize sidebars existentes de inbox e execucao para links `Inbox` `/inbox`, `Tarefas` `/execucao` e `Agenda` `/agenda`, destacando `pathname === "/agenda"`. Nao transforme `/execucao` na agenda completa.</action>
  <verify>
    <automated>rg "getAgendaItems|getAgendaItemById|getAgendaSummary|initialSummary|initialSelectedItem" "src/app/(workspace)/agenda/page.tsx" && rg "fetch\\(`/api/agenda/|fetch\\(\"/api/agenda|updateAgenda|onUpdateStatus|useTransition" src/components/agenda/workspace-shell.tsx && rg "href=\\\"/agenda\\\"|Agenda" src/components/inbox/workspace-shell.tsx src/components/execution/workspace-shell.tsx</automated>
  </verify>
  <done>`/agenda` existe como rota server-first e a navegacao principal inclui Inbox, Tarefas e Agenda.</done>
</task>

<task type="auto">
  <name>Task 2: Criar lista, filtros, detalhe, radar e estilos da agenda</name>
  <files>src/components/agenda/agenda-table.tsx, src/components/agenda/filters-bar.tsx, src/components/detail/agenda-detail-panel.tsx, src/components/dashboard/agenda-radar.tsx, src/components/agenda/workspace-shell.tsx, src/app/globals.css</files>
  <action>Crie `AgendaTable` seguindo `TasksTable`, com colunas visiveis para tipo, titulo, origem, responsavel, data/hora de vencimento e estado temporal; use chips compactos para `vencido`, `hoje`, `proximo`, `futuro` e destaque vencidos/hoje sem depender do detalhe. Crie `FiltersBar` com filtros por status, tipo, origem, estado temporal e ordenacao. Crie `AgendaDetailPanel` seguindo `TaskDetailPanel`, exibindo titulo, tipo, status, vencimento, responsavel, notas, snapshot da origem, timeline e acoes `Concluir`, `Reagendar` e `Cancelar` quando aplicavel. Reagendamento deve editar `dueAt` e nota mantendo origem imutavel. Crie `AgendaRadar` com contadores compactos para vencidos, hoje, proximos, abertos e uma lista curta de itens criticos. Atualize `src/app/globals.css` com classes `agenda-*` reaproveitando a paleta manual existente, sem calendario mensal/weekly grid e sem cards dentro de cards.</action>
  <verify>
    <automated>rg "Vencido|Hoje|Proximo|Futuro|Origem|Responsavel|Vencimento" src/components/agenda/agenda-table.tsx src/components/detail/agenda-detail-panel.tsx && rg "Concluir|Reagendar|Cancelar|Origem vinculada" src/components/detail/agenda-detail-panel.tsx && rg "overdue|today|near|critical|vencidos|proximos" src/components/dashboard/agenda-radar.tsx src/app/globals.css</automated>
  </verify>
  <done>A agenda renderiza lista densa, filtros, detalhe acionavel, radar temporal e estilos consistentes com a mesa de execucao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component -> Client Component | dados reais de agenda cruzam para estado interativo |
| Client Component -> API agenda | filtros, reagendamento e status partem do browser |
| UI -> operador | risco temporal precisa ser exibido sem ambiguidade para evitar decisao errada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | T | `src/components/agenda/workspace-shell.tsx` | mitigate | usar API como fonte de verdade apos mutacoes; nao atualizar status apenas localmente |
| T-03-12 | T | `AgendaDetailPanel` | mitigate | permitir PATCH somente de campos editaveis (`title`, `dueAt`, `ownerName`, `notes`, `kind`) e nunca origem/snapshot |
| T-03-13 | I | `AgendaTable` | accept | dados exibidos ja estao visiveis no workspace local sem auth; nao introduzir mascaramento falso |
| T-03-14 | D | `AgendaRadar` | mitigate | renderizar listas curtas e depender do resumo server-side para evitar UI pesada com arquivo crescendo |
| T-03-15 | R | `AgendaDetailPanel` | mitigate | exibir timeline do dominio para status/reagendamento em vez de mensagens efemeras apenas no cliente |
</threat_model>

<verification>
Executar os comandos `rg` das tarefas.
Ao final da fase, tambem executar `npm run lint`, `npm run build` e `npm run test -- --run`.
</verification>

<success_criteria>
- AGND-02: `/agenda` mostra follow-ups, prazos e compromissos em uma visao unica.
- AGND-03: vencidos, hoje e proximos aparecem na lista e no radar sem abrir detalhe.
- A navegacao principal inclui `Inbox`, `Tarefas` e `Agenda`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coordenacao-de-agenda/03-coordenacao-de-agenda-03-SUMMARY.md`
</output>
