---
phase: 03-coordenacao-de-agenda
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/components/agenda/schedule-dialog.tsx
  - src/components/inbox/workspace-shell.tsx
  - src/components/detail/item-detail-panel.tsx
  - src/components/execution/workspace-shell.tsx
  - src/components/detail/task-detail-panel.tsx
  - src/components/dashboard/execution-radar.tsx
  - src/test/agenda-workspace.test.tsx
  - src/test/execution-workspace.test.tsx
  - src/app/globals.css
autonomous: true
requirements_addressed:
  - AGND-01
  - AGND-02
  - AGND-03
must_haves:
  truths:
    - "Operador consegue agendar follow-up ou prazo a partir de uma entrada."
    - "Operador consegue agendar follow-up, prazo ou compromisso a partir de uma tarefa."
    - "`/execucao` mostra somente pontos contextuais de agenda, nao a agenda completa."
    - "Fluxo de agenda tem testes de workspace para filtros, selecao, criacao, reagendamento e conclusao."
    - "Suite completa de lint, build e testes passa."
  artifacts:
    - path: "src/components/agenda/schedule-dialog.tsx"
      provides: "Formulario reutilizavel de agendamento contextual."
    - path: "src/components/detail/item-detail-panel.tsx"
      provides: "Acao contextual para agendar a partir de inbox."
    - path: "src/components/detail/task-detail-panel.tsx"
      provides: "Acao contextual para agendar a partir de tarefa."
    - path: "src/test/agenda-workspace.test.tsx"
      provides: "Cobertura RTL da agenda."
  key_links:
    - from: "src/components/detail/item-detail-panel.tsx"
      to: "/api/agenda"
      via: "Agendamento contextual de inbox"
      pattern: "linkedType.*inbox"
    - from: "src/components/detail/task-detail-panel.tsx"
      to: "/api/agenda"
      via: "Agendamento contextual de tarefa"
      pattern: "linkedType.*task"
    - from: "src/components/agenda/workspace-shell.tsx"
      to: "src/test/agenda-workspace.test.tsx"
      via: "Fluxo interativo testado"
      pattern: "WorkspaceShell"
---

<objective>
Fechar a fase conectando a criacao contextual de agenda aos fluxos de inbox e tarefas, e cobrindo a experiencia com testes automatizados.

Purpose: cumprir AGND-01 sem transformar `/execucao` em agenda completa, e garantir que AGND-02/03 continuem verificaveis depois das integracoes.
Output: dialog de agendamento, acoes contextuais em detalhe de inbox/tarefa, ajustes de radar contextual e testes finais.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coordenacao-de-agenda/03-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@src/components/inbox/workspace-shell.tsx
@src/components/detail/item-detail-panel.tsx
@src/components/execution/workspace-shell.tsx
@src/components/detail/task-detail-panel.tsx
@src/components/dashboard/execution-radar.tsx
@src/test/execution-workspace.test.tsx

<interfaces>
O dialog deve emitir payload compativel com `createAgendaItemSchema`:
```ts
{
  linkedType: "inbox" | "task";
  linkedId: number;
  kind: "follow_up" | "deadline" | "commitment";
  title: string;
  dueAt: string;
  ownerName?: string | null;
  notes?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar agendamento contextual em inbox e tarefas</name>
  <files>src/components/agenda/schedule-dialog.tsx, src/components/inbox/workspace-shell.tsx, src/components/detail/item-detail-panel.tsx, src/components/execution/workspace-shell.tsx, src/components/detail/task-detail-panel.tsx, src/app/globals.css</files>
  <action>Crie `src/components/agenda/schedule-dialog.tsx` como formulario controlado reutilizavel, no padrao de `NewEntryDialog`, aceitando `linkedType`, `linkedId`, titulo sugerido, owner sugerido e callback `onSubmit`. Campos obrigatorios: tipo, titulo e `dueAt`; campos opcionais: responsavel e notas. Em `ItemDetailPanel`, adicione acao `Agendar follow-up` para entrada selecionada, chamando callback do shell. Em `TaskDetailPanel`, adicione acao `Agendar prazo` ou `Agendar compromisso` para tarefa selecionada, chamando callback do shell. Nos shells de inbox e execucao, gerencie abertura do dialog e POST `/api/agenda` com `linkedType: "inbox"` ou `"task"`, `linkedId` real e dados do form. Apos sucesso, mostre notice curto e mantenha o usuario na tela atual; a visao completa continua em `/agenda`. Atualize `ExecutionRadar` somente com um resumo pequeno de agenda se o plano conseguir ler `AgendaSummary` sem transformar a mesa em agenda completa; caso contrario, deixe apenas o CTA contextual e link para `/agenda`. Nao implemente compromissos independentes nem integracao externa.</action>
  <verify>
    <automated>rg "ScheduleDialog|Agendar follow-up|Agendar prazo|Agendar compromisso|/api/agenda|linkedType.*inbox|linkedType.*task" src/components/agenda/schedule-dialog.tsx src/components/inbox/workspace-shell.tsx src/components/detail/item-detail-panel.tsx src/components/execution/workspace-shell.tsx src/components/detail/task-detail-panel.tsx</automated>
  </verify>
  <done>Entradas e tarefas possuem acoes contextuais para criar agenda vinculada sem sair do fluxo operacional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir workspace de agenda e fluxos contextuais com testes</name>
  <files>src/test/agenda-workspace.test.tsx, src/test/execution-workspace.test.tsx</files>
  <behavior>
    - Teste 1: `/agenda` renderiza lista server-hydrated, radar e detalhe.
    - Teste 2: filtros por tipo/status/estado temporal escondem e mostram linhas corretamente.
    - Teste 3: selecionar item busca detalhe em `/api/agenda/[id]`.
    - Teste 4: concluir item chama `/api/agenda/[id]/status` e atualiza radar.
    - Teste 5: reagendar item chama PATCH `/api/agenda/[id]`.
    - Teste 6: agendamento contextual em `/execucao` chama POST `/api/agenda` com `linkedType: "task"`.
    - Teste 7: agendamento contextual em inbox chama POST `/api/agenda` com `linkedType: "inbox"` quando coberto por teste ou assertion de component contract.
  </behavior>
  <action>Crie `src/test/agenda-workspace.test.tsx` seguindo o padrao de `src/test/execution-workspace.test.tsx`, mockando `next/navigation`, dados de `AgendaListItem`, `AgendaDetail` e `AgendaSummary`, e `fetch`. Cubra renderizacao, filtros, selecao, concluir/cancelar e reagendar. Atualize `src/test/execution-workspace.test.tsx` para cobrir o botao/dialog de agendamento de tarefa e confirmar POST `/api/agenda` com `linkedType: "task"` e `linkedId` da tarefa selecionada. Se adicionar cobertura de inbox no mesmo arquivo for pesada, crie assertion no teste novo usando `ItemDetailPanel` isolado; nao deixe AGND-01 sem prova automatizada para origem inbox. Preserve os testes existentes da fase 2.</action>
  <verify>
    <automated>npm run test -- --run src/test/agenda-workspace.test.tsx src/test/execution-workspace.test.tsx</automated>
  </verify>
  <done>Fluxos principais da agenda e a criacao contextual estao cobertos por testes RTL sem quebrar a mesa de execucao.</done>
</task>

<task type="auto">
  <name>Task 3: Executar verificacao final da fase</name>
  <files>src/test/agenda-domain.test.ts, src/test/agenda-api.test.ts, src/test/agenda-workspace.test.tsx, src/test/execution-workspace.test.tsx</files>
  <action>Execute a verificacao completa obrigatoria da fase: `npm run lint`, `npm run build` e `npm run test -- --run`. Corrija qualquer regressao causada pelos planos 03-01 a 03-04, mantendo o escopo restrito a agenda, navegacao e pontos contextuais. Confirme tambem com `rg` que nao existem referencias a Google Calendar, Outlook, OAuth ou agenda independente sem origem obrigatoria.</action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>Lint, build e suite completa passam; nenhum item fora de escopo foi introduzido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detalhe inbox/task -> POST `/api/agenda` | usuario agenda a partir de superfice contextual com ids manipulaveis no cliente |
| dialog -> validacao server-side | formulario pode enviar data invalida ou tipo/status inesperado |
| testes -> regressao de fase anterior | mudancas de UI podem quebrar execucao de trabalho ja verificada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | T | `ScheduleDialog` | mitigate | enviar somente payload compativel com schema; confiar na API para validar origem real |
| T-03-17 | T | `ItemDetailPanel`/`TaskDetailPanel` | mitigate | usar `linkedId` do detalhe selecionado e nao permitir edicao manual de origem |
| T-03-18 | T | `dueAt` input | mitigate | exigir valor no formulario e deixar `createAgendaItemSchema` rejeitar formato invalido |
| T-03-19 | R | notices contextuais | accept | notices sao feedback de UX, mas a auditoria real fica na timeline persistida pelo dominio |
| T-03-20 | D | testes/workspace | mitigate | manter listas criticas compactas e nao renderizar agenda completa dentro de `/execucao` |
</threat_model>

<verification>
Executar:
- `npm run test -- --run src/test/agenda-workspace.test.tsx src/test/execution-workspace.test.tsx`
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
</verification>

<success_criteria>
- AGND-01: operador agenda a partir de inbox e tarefa por acoes contextuais.
- AGND-02: agenda unificada continua em `/agenda`, nao espalhada em `/execucao`.
- AGND-03: vencidos/proximos seguem evidentes depois de concluir, cancelar ou reagendar.
- Requisitos da fase 2 continuam preservados.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coordenacao-de-agenda/03-coordenacao-de-agenda-04-SUMMARY.md`
</output>
