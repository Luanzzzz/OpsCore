---
phase: 03-coordenacao-de-agenda
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/api/agenda/route.ts
  - src/app/api/agenda/[id]/route.ts
  - src/app/api/agenda/[id]/status/route.ts
  - src/test/agenda-api.test.ts
autonomous: true
requirements_addressed:
  - AGND-01
  - AGND-02
  - AGND-03
must_haves:
  truths:
    - "API cria itens de agenda somente quando a origem existe."
    - "API lista agenda, detalhe e resumo em shape consumivel pela UI."
    - "API permite reagendar, concluir e cancelar com validacao server-side."
    - "Payloads invalidos, ids invalidos e origem inexistente retornam codigos deterministicos."
  artifacts:
    - path: "src/app/api/agenda/route.ts"
      provides: "GET lista/resumo e POST criacao."
    - path: "src/app/api/agenda/[id]/route.ts"
      provides: "GET detalhe e PATCH metadados/reagendamento."
    - path: "src/app/api/agenda/[id]/status/route.ts"
      provides: "POST de conclusao/cancelamento."
  key_links:
    - from: "src/app/api/agenda/route.ts"
      to: "src/lib/validation/agenda.ts"
      via: "Parse de filtros e payload de criacao"
      pattern: "agendaFiltersSchema|createAgendaItemSchema"
    - from: "src/app/api/agenda/route.ts"
      to: "src/db/queries/agenda.ts"
      via: "Criacao e listagem"
      pattern: "createAgendaItem|getAgendaItems|getAgendaSummary"
    - from: "src/app/api/agenda/[id]/status/route.ts"
      to: "src/db/queries/agenda.ts"
      via: "Mutacao de status"
      pattern: "updateAgendaStatus"
---

<objective>
Expor a agenda por route handlers App Router seguindo os padroes de inbox e tarefas.

Purpose: permitir que a UI crie, liste, detalhe, reagende, conclua e cancele itens de agenda sem duplicar regra de dominio no cliente.
Output: rotas `/api/agenda`, `/api/agenda/[id]`, `/api/agenda/[id]/status` e testes de API.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coordenacao-de-agenda/03-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@src/app/api/tasks/route.ts
@src/app/api/tasks/[id]/route.ts
@src/app/api/tasks/[id]/status/route.ts
@src/app/api/inbox/[id]/convert/route.ts
@src/db/queries/agenda.ts
@src/lib/validation/agenda.ts

<interfaces>
Rotas devem seguir o padrao existente:
```ts
export async function GET(request: NextRequest) { ... }
export async function POST(request: NextRequest) { ... }

type RouteContext = {
  params: Promise<{ id: string }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar endpoints de lista, criacao e detalhe</name>
  <files>src/app/api/agenda/route.ts, src/app/api/agenda/[id]/route.ts, src/test/agenda-api.test.ts</files>
  <behavior>
    - Teste 1: `POST /api/agenda` cria item a partir de inbox e retorna 201.
    - Teste 2: `POST /api/agenda` cria item a partir de task e retorna snapshot da tarefa.
    - Teste 3: payload invalido retorna 400.
    - Teste 4: origem inexistente retorna 404.
    - Teste 5: duplicata ativa retorna 409.
    - Teste 6: `GET /api/agenda` retorna `{ items, summary }` com filtros opcionais.
    - Teste 7: `GET /api/agenda/[id]` retorna detalhe ou 404.
  </behavior>
  <action>Implemente `src/app/api/agenda/route.ts` com `GET` e `POST`. `GET` deve usar `agendaFiltersSchema.safeParse` para query params `status`, `kind`, `linkedType`, `urgencyState` e `sort`, retornando `{ items, summary }`; erro de filtro retorna 400. `POST` deve ler JSON com fallback seguro, validar `createAgendaItemSchema`, chamar `createAgendaItem` e mapear resultados: sucesso 201, `linked-origin-not-found` 404, `active-duplicate` 409, payload invalido 400. Implemente `src/app/api/agenda/[id]/route.ts` com parse de id positivo, `GET` de detalhe e `PATCH` para `updateAgendaItem`; ids invalidos retornam 400 e inexistentes retornam 404. Use mensagens de erro em ingles tecnico como as rotas atuais (`Invalid agenda payload`, `Agenda item not found`) para manter padrao de API. Nao confie em snapshot enviado pelo cliente.</action>
  <verify>
    <automated>npm run test -- --run src/test/agenda-api.test.ts</automated>
  </verify>
  <done>API lista, cria e detalha agenda com codigos deterministas e validacao centralizada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar endpoint dedicado de status para concluir e cancelar</name>
  <files>src/app/api/agenda/[id]/status/route.ts, src/test/agenda-api.test.ts</files>
  <behavior>
    - Teste 1: `POST /api/agenda/[id]/status` conclui item aberto.
    - Teste 2: `POST /api/agenda/[id]/status` cancela item aberto com nota opcional.
    - Teste 3: enum de status invalido retorna 400.
    - Teste 4: id inexistente retorna 404.
    - Teste 5: resumo deixa de contar item concluido/cancelado como vencido ou proximo.
  </behavior>
  <action>Implemente `src/app/api/agenda/[id]/status/route.ts` no mesmo modelo de `src/app/api/tasks/[id]/status/route.ts`: parse de id positivo, leitura JSON segura, `updateAgendaStatusSchema.safeParse`, chamada a `updateAgendaStatus` e retorno 200/400/404. Permita transicoes para `Aberto`, `Concluido` e `Cancelado`, mas mantenha regra de dominio em `queries/agenda.ts` para atualizar `completedAt`, `cancelledAt` e timeline. O endpoint nao deve aceitar alteracao de `linkedType`, `linkedId` ou snapshot.</action>
  <verify>
    <automated>npm run test -- --run src/test/agenda-api.test.ts</automated>
  </verify>
  <done>Mutacoes de ciclo de vida da agenda passam por endpoint dedicado, validado e testado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/agenda` | criacao/listagem com filtros vindos de input nao confiavel |
| browser -> `/api/agenda/[id]` | detalhe e reagendamento por id manipulavel |
| browser -> `/api/agenda/[id]/status` | mutacao de status por payload manual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | T | `src/app/api/agenda/route.ts` | mitigate | usar Zod safeParse para criacao e filtros; recusar payload invalido com 400 |
| T-03-07 | T | `src/app/api/agenda/route.ts` | mitigate | mapear origem inexistente para 404 e duplicata ativa para 409 sem persistir |
| T-03-08 | T | `src/app/api/agenda/[id]/route.ts` | mitigate | validar id positivo e impedir PATCH de origem/snapshot |
| T-03-09 | T | `src/app/api/agenda/[id]/status/route.ts` | mitigate | validar enum de status e delegar timeline/status para dominio |
| T-03-10 | D | route handlers agenda | mitigate | rejeitar filtros/payloads invalidos cedo para evitar crescimento ou processamento desnecessario |
</threat_model>

<verification>
Executar `npm run test -- --run src/test/agenda-api.test.ts`.
Ao final da fase, tambem executar `npm run lint`, `npm run build` e `npm run test -- --run`.
</verification>

<success_criteria>
- AGND-01: API permite criar agenda a partir de entrada ou tarefa.
- AGND-02: API expõe lista unificada com resumo para `/agenda`.
- AGND-03: API preserva estados temporais e status para destacar ou remover itens criticos.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coordenacao-de-agenda/03-coordenacao-de-agenda-02-SUMMARY.md`
</output>
