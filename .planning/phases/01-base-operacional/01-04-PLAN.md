---
phase: 01-base-operacional
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - drizzle.config.ts
  - src/db/schema/inbox.ts
  - src/db/queries/inbox.ts
  - src/lib/validation/inbox.ts
  - src/types/inbox.ts
  - src/app/api/inbox/route.ts
  - src/app/api/inbox/[id]/route.ts
  - src/test/inbox-domain.test.ts
  - src/test/inbox-api.test.ts
autonomous: true
requirements:
  - INBX-01
  - INBX-02
must_haves:
  truths:
    - "O sistema consegue registrar novas entradas operacionais em uma fila unica persistida."
    - "Cada entrada registrada preserva origem, contexto basico, status e prioridade revisada inicial."
    - "A fila nasce com estrutura canonica suficiente para alimentar triagem, dashboard e fases futuras."
    - "O estado assincrono da triagem fica formalizado como `triageStatus`, evitando divergencia entre API e UI."
  artifacts:
    - path: "src/db/schema/inbox.ts"
      provides: "Modelo fonte-de-verdade do inbox com separacao entre dados brutos, sugestoes de IA e campos revisados."
    - path: "src/types/inbox.ts"
      provides: "Contratos compartilhados do dominio do inbox, triagem e dashboard."
    - path: "src/app/api/inbox/route.ts"
      provides: "Entrada HTTP para criar e listar itens da fila unica."
  key_links:
    - from: "src/app/api/inbox/route.ts"
      to: "src/lib/validation/inbox.ts"
      via: "Validacao Zod antes de qualquer insert/update"
      pattern: "parse|safeParse"
    - from: "src/app/api/inbox/route.ts"
      to: "src/db/queries/inbox.ts"
      via: "Mutacoes e listagem da fila"
      pattern: "createInboxItem|getInboxItems"
    - from: "src/app/api/inbox/[id]/route.ts"
      to: "src/db/queries/inbox.ts"
      via: "Leitura detalhada do item selecionado"
      pattern: "getInboxItemById"
    - from: "src/db/queries/inbox.ts"
      to: "src/db/schema/inbox.ts"
      via: "Queries baseadas no schema canonico"
      pattern: "inboxItems"
---

<objective>
Definir os contratos canonicos do inbox, a camada de persistencia inicial e as APIs basicas de cadastro/listagem/detalhe.

Purpose: travar desde cedo a fila unica como source of truth e impedir que a fase derive para mock, placeholder ou mistura entre sugestao de IA e valor revisado.
Output: schema relacional do inbox, queries basicas e APIs de cadastro/listagem/detalhe prontas para sustentar triagem e UI.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-base-operacional/01-CONTEXT.md
@.planning/phases/01-base-operacional/01-UI-SPEC.md
@.planning/phases/01-base-operacional/01-RESEARCH.md
@.planning/phases/01-base-operacional/01-VALIDATION.md
@context.md
@visao-do-produto.md
@arquitetura-macro.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir os contratos canonicos do inbox e a camada de persistencia inicial</name>
  <files>drizzle.config.ts, src/db/schema/inbox.ts, src/db/queries/inbox.ts, src/lib/validation/inbox.ts, src/types/inbox.ts, src/test/inbox-domain.test.ts</files>
  <behavior>
    - Test 1: criar uma entrada sem prioridade explicita persiste `Media` como valor revisado padrao per D-05.
    - Test 2: criar uma entrada sem status explicito persiste `Nova` como estado inicial per D-04.
    - Test 3: sugestoes de IA ficam em campos `ai_*` separados de `*_reviewed`, sem sobrescrever a verdade operacional per D-07.
    - Test 4: `triageStatus` nasce como `pending`, muda para `ready` quando a sugestao chega e pode evoluir para `reviewed` apos a confirmacao humana.
  </behavior>
  <action>Crie o modelo canonico `inbox_items` como fonte de verdade do produto. O schema deve implementar per D-02/D-03/D-04/D-05 os campos minimos da fila (`title`, `source`, `summaryShort`, `status`, `priorityReviewed`, `waitingOnResponse`, `lastActivityAt`) e separar explicitamente entrada bruta, sugestoes da IA (`aiCategory`, `aiUrgency`, `aiNextAction`, `aiSummary`, `aiRationale`) e campos revisados por humano. Formalize tambem `triageStatus` como contrato persistido com enum canonico (`pending`, `ready`, `reviewed`) para unificar o ciclo assíncrono entre API e UI. Em `src/types/inbox.ts`, exporte enums e DTOs reutilizaveis para lista, detalhe e dashboard incluindo `triageStatus`. Em `src/db/queries/inbox.ts`, adicione helpers de criacao, listagem e leitura por id ja preparados para ordenacao canonica por prioridade e envelhecimento per D-06. Evite qualquer campo ou fluxo que dependa de tarefas, agenda ou integracoes reais, pois esses itens estao deferidos.</action>
  <verify>
    <automated>npm run lint && npm run test -- --run src/test/inbox-domain.test.ts</automated>
  </verify>
  <done>Existe um modelo persistente e tipado do inbox com defaults corretos, separacao entre sugestao e revisao humana, `triageStatus` canonico e contratos estaveis para as proximas ondas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor cadastro, listagem e detalhe da fila unica por APIs de dominio</name>
  <files>src/app/api/inbox/route.ts, src/app/api/inbox/[id]/route.ts, src/test/inbox-api.test.ts</files>
  <behavior>
    - Test 1: `POST /api/inbox` aplica defaults de status/prioridade e registra `source` como atributo operacional.
    - Test 2: `GET /api/inbox` devolve os campos minimos da linha com ordenacao coerente com D-06.
    - Test 3: `GET /api/inbox/[id]` devolve `InboxDetail` completo para o painel lateral sem depender de dados mockados no cliente.
  </behavior>
  <action>Implemente as rotas do dominio para criar, listar e detalhar entradas do inbox usando os contratos do Task 1. A rota de criacao deve validar payload com Zod, preencher defaults de status/prioridade per D-04/D-05, aceitar `source` como atributo operacional per D-03 mesmo sem integracao real e inicializar `triageStatus` como `pending`. A rota de listagem deve retornar os campos minimos da linha per D-02 e suportar ordenacao/filtros server-driven coerentes com D-06. A rota de detalhe deve devolver `InboxDetail` para o item selecionado, incluindo `triageStatus`, preparando o consumo do painel lateral sem divergencia entre refresh e estado local.</action>
  <verify>
    <automated>npm run test -- --run src/test/inbox-api.test.ts && npm run build</automated>
  </verify>
  <done>O app consegue criar, listar e detalhar entradas reais da fila unica por meio de APIs tipadas e validadas, prontas para alimentar UI, triagem e dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API inbox | payload do operador cruza a fronteira e nao pode ser confiado |
| API inbox→database | dados validados viram registros canonicos da operacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | `src/app/api/inbox/route.ts` | mitigate | validar enums, strings e campos obrigatorios com Zod antes de chamar qualquer query |
| T-01-04 | T | `src/db/queries/inbox.ts` | mitigate | usar Drizzle/query builder parametrizado; nao concatenar filtros manualmente |
| T-01-05 | I | `src/app/api/inbox/[id]/route.ts` | mitigate | devolver apenas o contrato detalhado necessario ao painel lateral, sem segredos nem payload bruto sensivel |
</threat_model>

<verification>
Executar `npm run lint`, `npm run test -- --run src/test/inbox-domain.test.ts`, `npm run test -- --run src/test/inbox-api.test.ts` e `npm run build`. Confirmar tambem que a listagem inicial do inbox retorna dados reais do banco e nao arrays mockados no componente.
</verification>

<success_criteria>
- A fila unica esta modelada como fonte de verdade persistida.
- APIs de cadastro/listagem/detalhe devolvem contratos coerentes com os requisitos INBX-01 e INBX-02.
- Nenhum campo de sugestao da IA sobrescreve o valor revisado pelo operador.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-operacional/01-base-operacional-04-SUMMARY.md`
</output>
