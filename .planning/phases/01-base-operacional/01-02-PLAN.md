---
phase: 01-base-operacional
plan: 02
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - src/lib/triage/schema.ts
  - src/lib/triage/prompt.ts
  - src/lib/triage/service.ts
  - src/app/api/triage/route.ts
  - src/app/api/inbox/[id]/review/route.ts
  - src/db/queries/dashboard.ts
  - src/app/api/dashboard/route.ts
  - src/test/triage-service.test.ts
  - src/test/dashboard-review.test.ts
autonomous: true
requirements:
  - TRIA-01
  - TRIA-02
  - TRIA-03
  - TRIA-04
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "Cada entrada pode receber sugestoes editaveis de categoria, urgencia, proxima acao e resumo contextual."
    - "O operador consegue revisar, aceitar, ajustar ou ignorar a triagem da IA sem perder o historico."
    - "O dashboard resume gargalos e prioridades a partir de dados persistidos reais da fila."
  artifacts:
    - path: "src/lib/triage/service.ts"
      provides: "Pipeline de triagem estruturada via Responses API com JSON Schema."
    - path: "src/app/api/inbox/[id]/review/route.ts"
      provides: "Mutacao de revisao humana separada da sugestao automatica."
    - path: "src/db/queries/dashboard.ts"
      provides: "Agregados operacionais alimentados pela tabela canonica do inbox."
  key_links:
    - from: "src/app/api/triage/route.ts"
      to: "src/lib/triage/service.ts"
      via: "Geracao de sugestoes estruturadas"
      pattern: "triageInboxItem"
    - from: "src/app/api/triage/route.ts"
      to: "src/app/api/inbox/[id]/route.ts"
      via: "Reprocessar a triagem e devolver o estado detalhado atualizado do item"
      pattern: "getInboxItemById|pending|ready"
    - from: "src/lib/triage/service.ts"
      to: "src/db/schema/inbox.ts"
      via: "Persistencia em campos `ai_*`"
      pattern: "aiCategory|aiUrgency|aiNextAction|aiSummary|aiRationale"
    - from: "src/app/api/dashboard/route.ts"
      to: "src/db/queries/dashboard.ts"
      via: "Leitura de widgets operacionais"
      pattern: "getDashboardSummary"
user_setup:
  - service: openai
    why: "Gerar sugestoes estruturadas de triagem na fase 1"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform -> API keys"
---

<objective>
Implementar o pipeline de triagem assistida por IA e os agregados operacionais do dashboard, ambos apoiados em dados persistidos reais.

Purpose: entregar o nucleo de inteligencia assistida da fase sem romper o humano no loop e sem cair em dashboard de placeholder.
Output: servico de triagem estruturada, revisao humana separada e consultas de dashboard que respondem o que exige atencao agora.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-base-operacional/01-CONTEXT.md
@.planning/phases/01-base-operacional/01-UI-SPEC.md
@.planning/phases/01-base-operacional/01-RESEARCH.md

<interfaces>
From src/types/inbox.ts:
```typescript
export type InboxStatus = 'Nova' | 'Em analise' | 'Aguardando resposta' | 'Concluida/Arquivada';
export type InboxPriority = 'Baixa' | 'Media' | 'Alta' | 'Critica';
export interface InboxListItem {
  id: number;
  title: string;
  source: string;
  summaryShort: string;
  status: InboxStatus;
  priorityReviewed: InboxPriority;
  waitingOnResponse: boolean;
  lastActivityAt: string;
}
export interface TriageSuggestion {
  category: string;
  urgency: InboxPriority;
  nextAction: string;
  summary: string;
  rationale: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construir a camada de triagem estruturada e persistencia segura das sugestoes</name>
  <files>src/lib/triage/schema.ts, src/lib/triage/prompt.ts, src/lib/triage/service.ts, src/app/api/triage/route.ts, src/test/triage-service.test.ts</files>
  <behavior>
    - Test 1: a triagem retorna exatamente `category`, `urgency`, `nextAction`, `summary` e `rationale`.
    - Test 2: `urgency` aceita apenas `Baixa`, `Media`, `Alta` ou `Critica` per D-05.
    - Test 3: gerar triagem nao altera `priorityReviewed` nem outros campos revisados per D-07.
  </behavior>
  <action>Implemente o pipeline de triagem usando Responses API + Structured Outputs, com JSON Schema fechado e mapeamento para os campos `ai_*` definidos no Plan 04. O prompt deve produzir per D-08 sugestoes de categoria, urgencia, proxima acao e resumo contextual, e per D-09 uma justificativa curta. Exponha um endpoint para disparar/reprocessar a triagem de um item sem bloquear o cadastro inicial: a entrada nasce com `triageStatus = pending` e, quando a sugestao for persistida, evolui para `ready`. Esse endpoint deve estar ligado ao fluxo real do item: ao final da chamada ele precisa persistir a triagem, atualizar `triageStatus`, devolver o estado detalhado atualizado e deixar claro como a UI ou o fluxo server-side saberá sair de `pending` para `ready`. Nao introduza autonomia, resposta automatica ou qualquer efeito colateral fora do proprio item.</action>
  <verify>
    <automated>npm run lint && npm run test -- --run src/test/triage-service.test.ts && npm run build</automated>
  </verify>
  <done>O sistema consegue gerar sugestoes estruturadas e persisti-las em campos dedicados, mantendo a fronteira clara entre recomendacao da IA e estado operacional revisado.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar revisao humana e agregados operacionais do dashboard</name>
  <files>src/app/api/inbox/[id]/review/route.ts, src/db/queries/dashboard.ts, src/app/api/dashboard/route.ts, src/test/dashboard-review.test.ts</files>
  <action>Implemente a mutacao de revisao humana que aceite, ajuste ou ignore sugestoes per D-07/D-08, gravando `*_reviewed`, flags de revisao e timestamps sem apagar a sugestao original. Ao concluir a revisao, atualize `triageStatus` para `reviewed`. Em paralelo, crie queries de dashboard baseadas somente no banco para os blocos obrigatorios per D-10/D-11: entradas por status, itens Alta/Critica, itens aguardando resposta, itens sem triagem revisada e itens mais antigos/tempo medio sem resposta. O endpoint do dashboard deve devolver contratos prontos para widgets pequenos e acionaveis, sem graficos analiticos extras e sem calcular indicadores a partir de arrays mockados no cliente. Garanta tambem que um item reprocessado pela triagem altere o detalhe e os agregados relevantes quando apropriado.</action>
  <verify>
    <automated>npm run test -- --run src/test/dashboard-review.test.ts && npm run build</automated>
  </verify>
  <done>Existe um fluxo completo de triagem assistida com revisao humana e um dashboard operacional baseado em consultas reais da fila, cobrindo TRIA-01..TRIA-04 e DASH-01..DASH-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API triage→OpenAI | texto operacional e instrucoes saem do dominio local para o provedor |
| OpenAI response→dominio interno | resposta do modelo cruza de volta para o schema da aplicacao |
| browser→API review | operador pode forcar enums e estados invalidos no payload manual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `src/lib/triage/service.ts` | mitigate | usar Structured Outputs estritos; rejeitar qualquer payload fora do schema antes de persistir |
| T-01-05 | I | `src/app/api/triage/route.ts` | mitigate | executar chamadas OpenAI somente no servidor com chave em env e sem logar payload sensivel em texto bruto |
| T-01-06 | T | `src/app/api/inbox/[id]/review/route.ts` | mitigate | validar enums/status/prioridade com Zod e registrar revisao humana separadamente dos campos `ai_*` |
| T-01-07 | R | `src/db/queries/dashboard.ts` | mitigate | derivar widgets de colunas persistidas e timestamps reais para evitar contestacao sobre a origem dos indicadores |
</threat_model>

<verification>
Executar `npm run lint` e `npm run build`. Validar manualmente via API que um item triado continua com sugestao original preservada apos ajuste humano e que o dashboard muda quando a revisao/status mudam.
</verification>

<success_criteria>
- O pipeline de triagem gera e persiste categoria, urgencia, proxima acao, resumo e justificativa.
- O operador consegue revisar a triagem sem perder a trilha da sugestao inicial.
- O dashboard responde com agregados reais e acionaveis, sem depender de mocks ou calculos client-side ad hoc.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-operacional/01-base-operacional-02-SUMMARY.md`
</output>
