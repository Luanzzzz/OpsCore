---
phase: 01-base-operacional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
  - next.config.ts
  - tsconfig.json
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/test/setup.ts
  - src/test/factories/inbox.ts
  - src/test/inbox-domain.test.ts
  - src/test/inbox-api.test.ts
  - .env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "O repositorio passa a sustentar um app executavel de Fase 1 sem quebrar o workspace documental existente."
    - "As ondas seguintes conseguem validar inbox, triagem e UI em cima de scripts reais do projeto, nao de setup ad hoc."
    - "Quem executar a fase consegue instalar, rodar e verificar o app usando comandos documentados e consistentes."
  artifacts:
    - path: "package.json"
      provides: "Scripts e dependencias base do app e da suite de testes."
    - path: "vitest.config.ts"
      provides: "Runner de testes inicial para a fase."
    - path: "README.md"
      provides: "Guia atualizado para o repositorio hibrido docs + app."
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "Script `npm run test` ligado ao runner escolhido"
      pattern: "test|vitest"
    - from: "README.md"
      to: "package.json"
      via: "Documentacao dos comandos reais do workspace"
      pattern: "npm run dev|npm run lint|npm run build|npm run test"
---

<objective>
Estabelecer a fundacao executavel da Fase 1: app web, tooling e infraestrutura de validacao minima.

Purpose: preparar um baseline confiavel para que os planos de dominio, triagem e UI possam ser executados sem misturar bootstrap com logica operacional.
Output: projeto bootstrapado, scripts/documentacao atualizados e base de testes pronta para sustentar as ondas seguintes.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-base-operacional/01-CONTEXT.md
@.planning/phases/01-base-operacional/01-UI-SPEC.md
@.planning/phases/01-base-operacional/01-RESEARCH.md
@context.md
@visao-do-produto.md
@arquitetura-macro.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrapar o app web preservando o workspace documental</name>
  <files>README.md, package.json, next.config.ts, tsconfig.json, vitest.config.ts, src/app/layout.tsx, src/app/globals.css, src/test/setup.ts, src/test/factories/inbox.ts, src/test/inbox-domain.test.ts, src/test/inbox-api.test.ts, .env.example</files>
  <action>Inicialize o app no proprio repositorio como monolito modular Next.js App Router per A1/A2 da pesquisa, sem mover ou apagar os documentos existentes. Configure `IBM Plex Sans`, tokens do contrato visual (`#F4F1EA`, `#D9E2D3`, `#C65D2E`, escala de espacamento em multiplos de 4) e um shell base que suporte a experiencia inbox-first per D-12, mas sem antecipar a UI completa desta fase. Inclua `.env.example` com `DATABASE_URL` e `OPENAI_API_KEY` documentados; nao exponha segredos no cliente. Nesta mesma task, instale a base minima de testes com `vitest`, adicione o script `npm run test`, crie `vitest.config.ts`, setup compartilhado e os primeiros arquivos de teste do dominio/API do inbox para que os tasks TDD da fase tenham infraestrutura real. Atualize `README.md` para registrar os novos comandos (`npm install`, `npm run dev`, `npm run lint`, `npm run build`, `npm run test`) e explicar que o repositório passa a combinar workspace documental + app web sem quebrar a navegação atual.</action>
  <verify>
    <automated>npm run lint && npm run build && npm run test -- --run</automated>
  </verify>
  <done>O repositório passa a ter um app executavel com App Router, estilos globais coerentes com o UI contract, scripts basicos funcionando sem quebrar os documentos existentes, base minima de testes pronta e documentação atualizada para o novo tooling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation→tooling | scripts e dependencias novas entram no workspace e precisam ser controlados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `package.json` / scripts | mitigate | manter scripts explicitos e documentados; nao introduzir watch mode como verify padrao |
| T-01-02 | I | `.env.example` / app shell | mitigate | manter `OPENAI_API_KEY` apenas no servidor e nunca repassar segredos via props client-side |
</threat_model>

<verification>
Executar `npm run lint`. Confirmar tambem que o bootstrap nao quebrou o workspace documental e que os comandos descritos no `README.md` batem com o `package.json`.
</verification>

<success_criteria>
- Existe um app bootstrapado pronto para receber as proximas ondas.
- A base de testes e scripts esta pronta antes da entrada no dominio.
- O repositório documenta corretamente o novo tooling introduzido.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-operacional/01-base-operacional-01-SUMMARY.md`
</output>
