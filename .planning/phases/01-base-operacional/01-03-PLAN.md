---
phase: 01-base-operacional
plan: 03
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-02
files_modified:
  - src/app/(workspace)/inbox/page.tsx
  - src/components/inbox/workspace-shell.tsx
  - src/components/inbox/inbox-table.tsx
  - src/components/inbox/filters-bar.tsx
  - src/components/inbox/new-entry-dialog.tsx
  - src/components/detail/item-detail-panel.tsx
  - src/components/detail/triage-review-card.tsx
  - src/components/dashboard/ops-radar.tsx
  - src/components/dashboard/status-block.tsx
  - src/app/(workspace)/inbox/loading.tsx
  - src/app/(workspace)/inbox/error.tsx
  - src/test/inbox-workspace.test.tsx
  - src/test/triage-review-ui.test.tsx
  - src/test/ops-radar.test.tsx
autonomous: true
requirements:
  - INBX-01
  - INBX-02
  - INBX-03
  - TRIA-01
  - TRIA-02
  - TRIA-03
  - TRIA-04
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "O operador enxerga uma fila densa com detalhe lateral e sinais operacionais claros sem abrir telas desnecessarias."
    - "A fila pode ser ordenada e filtrada por prioridade e estado operacional, com coerencia entre UI e servidor."
    - "O detalhe do item mostra sugestoes de IA editaveis e o dashboard-resumo destaca gargalos reais."
  artifacts:
    - path: "src/app/(workspace)/inbox/page.tsx"
      provides: "Tela principal inbox-first com dados reais"
    - path: "src/components/inbox/inbox-table.tsx"
      provides: "Lista densa com sorting/filtering coerentes com o dominio"
    - path: "src/components/inbox/new-entry-dialog.tsx"
      provides: "Fluxo de registro de nova entrada ligado ao POST /api/inbox"
    - path: "src/components/detail/triage-review-card.tsx"
      provides: "Revisao humana das sugestoes da IA"
    - path: "src/components/dashboard/ops-radar.tsx"
      provides: "Dashboard pequeno e orientado a acao"
  key_links:
    - from: "src/app/(workspace)/inbox/page.tsx"
      to: "src/app/api/inbox/route.ts"
      via: "Fetch server-side inicial da fila"
      pattern: "getInboxItems|fetch"
    - from: "src/components/detail/item-detail-panel.tsx"
      to: "src/app/api/inbox/[id]/route.ts"
      via: "Carregamento do detalhe completo do item selecionado"
      pattern: "getInboxItemById|InboxDetail"
    - from: "src/components/inbox/new-entry-dialog.tsx"
      to: "src/app/api/inbox/route.ts"
      via: "Cadastro de nova entrada operacional pela CTA principal"
      pattern: "POST /api/inbox|createInboxItem"
    - from: "src/components/detail/triage-review-card.tsx"
      to: "src/app/api/triage/route.ts"
      via: "Disparo ou reprocessamento explicito da triagem assíncrona do item"
      pattern: "triage|pending|ready"
    - from: "src/components/detail/triage-review-card.tsx"
      to: "src/app/api/inbox/[id]/review/route.ts"
      via: "Aceitar, ajustar ou ignorar sugestoes"
      pattern: "review"
    - from: "src/components/dashboard/ops-radar.tsx"
      to: "src/app/api/dashboard/route.ts"
      via: "Renderizacao do radar operacional"
      pattern: "dashboard"
---

<objective>
Entregar a experiencia inbox-first da Fase 1 com lista densa, painel de detalhe e dashboard-resumo totalmente ligados aos dados reais da operacao.

Purpose: provar o valor operacional do OpsCore na interface principal, respeitando integralmente o contrato visual aprovado e os requisitos de triagem e visibilidade.
Output: tela principal navegavel, lista filtravel/ordenavel, detalhe com revisao de IA, dashboard-resumo e estados de UX completos.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-base-operacional/01-CONTEXT.md
@.planning/phases/01-base-operacional/01-UI-SPEC.md
@.planning/phases/01-base-operacional/01-RESEARCH.md

<interfaces>
From src/types/inbox.ts:
```typescript
export interface InboxDetail extends InboxListItem {
  descriptionRaw: string;
  triageReviewedAt?: string | null;
  aiSuggestion?: TriageSuggestion | null;
  reviewedCategory?: string | null;
  reviewedNextAction?: string | null;
}
export interface DashboardSummary {
  byStatus: Array<{ status: InboxStatus; count: number }>;
  highPriorityCount: number;
  waitingOnResponseCount: number;
  unreviewedTriageCount: number;
  oldestItems: Array<{ id: number; title: string; ageHours: number }>;
  averageResponseAgeHours: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construir a tela principal inbox-first no layout list-detail definido</name>
  <files>src/app/(workspace)/inbox/page.tsx, src/components/inbox/workspace-shell.tsx, src/components/inbox/inbox-table.tsx, src/components/inbox/filters-bar.tsx, src/components/detail/item-detail-panel.tsx, src/test/inbox-workspace.test.tsx</files>
  <action>Implemente a tela principal per D-01/D-12 e pelo `01-UI-SPEC.md`: sidebar compacta, header funcional, lista densa como foco principal, painel de detalhe como ancora secundaria e dashboard-resumo como zona terciaria. A tabela deve mostrar obrigatoriamente per D-02 `titulo`, `origem`, `resumo curto`, `prioridade`, `status`, `aguardando resposta` e `ultima atividade`. Use TanStack Table para sorting/filtering controlados, com ordenacao padrao por prioridade e depois envelhecimento/ultima atividade per D-06. O painel lateral deve consumir `InboxDetail` real do item selecionado via `/api/inbox/[id]` ou query server-side equivalente, sem inventar estado detalhado apenas no cliente. Nao use cards empilhados como estrutura dominante e nao esconda filtros/ordenacao em menus profundos.</action>
  <verify>
    <automated>npm run lint && npm run test -- --run src/test/inbox-workspace.test.tsx && npm run build</automated>
  </verify>
  <done>A tela principal reflete o contrato de UI aprovado, mostra a fila unica com leitura rapida e mantem o detalhe do item selecionado sem perder o contexto da lista.</done>
</task>

<task type="auto">
  <name>Task 2: Entregar o fluxo de registro de nova entrada na interface principal</name>
  <files>src/app/(workspace)/inbox/page.tsx, src/components/inbox/workspace-shell.tsx, src/components/inbox/new-entry-dialog.tsx</files>
  <action>Implemente a CTA primária `Registrar entrada` como fluxo real de captura de entrada operacional, nao apenas como affordance visual. O fluxo deve abrir um formulario/modal/drawer com os campos minimos necessarios para o primeiro cadastro (`title`, `source`, `summaryShort` e dados basicos opcionais), validar a entrada na UI e enviar para `POST /api/inbox`, atualizando a lista apos sucesso. Esse cadastro deve respeitar a decisao de triagem assíncrona resolvida no `01-RESEARCH.md`: a entrada nasce persistida com estado operacional valido e `triageStatus = pending` ate que a propria interface ofereca um disparo claro de triagem ou reprocessamento via `/api/triage`, atualizando lista, detalhe e dashboard quando a sugestao voltar.</action>
  <verify>
    <automated>npm run lint && npm run test -- --run src/test/inbox-workspace.test.tsx && npm run build</automated>
  </verify>
  <done>O usuario consegue registrar uma entrada operacional pela interface principal e ver o item aparecer na fila unica sem depender de seed manual, script ou chamada API externa.</done>
</task>

<task type="auto">
  <name>Task 3: Exibir triagem editavel e o radar operacional com dados reais</name>
  <files>src/components/detail/triage-review-card.tsx, src/components/dashboard/ops-radar.tsx, src/components/dashboard/status-block.tsx, src/test/triage-review-ui.test.tsx, src/test/ops-radar.test.tsx</files>
  <action>No painel de detalhe, renderize per D-08/D-09 as sugestoes de categoria, urgencia, proxima acao, resumo contextual e justificativa curta, com affordances claras para aceitar, ajustar ou ignorar. No dashboard, limite-se aos blocos previstos em D-11 e no UI contract: entradas por status, itens Alta/Critica, itens aguardando resposta, itens sem triagem revisada e itens mais antigos/tempo medio sem resposta. Os widgets devem consumir exclusivamente os dados agregados da onda anterior; nao calcule indicadores via mock, fixture ou recontagem improvisada no cliente.</action>
  <verify>
    <automated>npm run test -- --run src/test/triage-review-ui.test.tsx src/test/ops-radar.test.tsx && npm run build</automated>
  </verify>
  <done>O operador consegue revisar a triagem diretamente no detalhe e o dashboard-resumo evidencia gargalos, urgencias e pendencias a partir do estado real da fila.</done>
</task>

<task type="auto">
  <name>Task 4: Fechar estados de experiencia, loading e erro com copy operacional consistente</name>
  <files>src/app/(workspace)/inbox/loading.tsx, src/app/(workspace)/inbox/error.tsx</files>
  <action>Implemente estados de loading, vazio e erro coerentes com o `01-UI-SPEC.md`, incluindo a CTA primaria `Registrar entrada`, o texto de empty state e a mensagem de erro operacional definida no contrato. O estado vazio deve empurrar o usuario para a proxima acao; loading deve usar skeletons discretos na lista, detalhe e dashboard; erro deve oferecer recuperacao clara sem linguagem de help desk generica. Preserve acessibilidade: labels textuais em icones isolados, foco visivel com accent reservado e contraste alto nos badges.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>A experiencia da Fase 1 fica completa e coerente nos estados essenciais, mantendo a narrativa de plataforma operacional madura e pronta para validacao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-render→client interativo | dados reais da fila saem do servidor e passam a sofrer filtros/selecoes locais |
| browser→API review | edicoes do operador atravessam a UI e alteram o estado revisado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | T | `src/components/inbox/inbox-table.tsx` | mitigate | alinhar sorting/filtering client-side aos mesmos campos canonicos retornados pelo servidor; nao inventar ordem paralela na UI |
| T-01-09 | I | `src/components/detail/item-detail-panel.tsx` | mitigate | exibir somente campos operacionais necessarios; nao renderizar segredos, prompts internos ou chaves de ambiente |
| T-01-10 | D | `src/components/dashboard/ops-radar.tsx` | mitigate | limitar o dashboard a 4-5 blocos pequenos e consultar agregados prontos, evitando render pesado e BI prematuro |
</threat_model>

<verification>
Executar `npm run lint` e `npm run build`. Validar no navegador que a lista abre com densidade alta, o detalhe troca sem recarregar a pagina, filtros/ordenacao funcionam, a CTA `Registrar entrada` aparece no vazio e o dashboard reflete alteracoes reais de status/revisao.
</verification>

<success_criteria>
- A interface principal segue o contrato de UI aprovado sem desvio para cards ou BI pesado.
- O usuario consegue registrar, visualizar, ordenar e filtrar a fila unica com contexto suficiente para triagem.
- O detalhe mostra a triagem editavel da IA e o dashboard resume gargalos reais da operacao.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-operacional/01-base-operacional-03-SUMMARY.md`
</output>
