---
phase: 02-execucao-de-trabalho
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/components/execution/tasks-table.tsx
  - src/components/execution/filters-bar.tsx
  - src/components/execution/conversion-strip.tsx
  - src/components/detail/task-detail-panel.tsx
  - src/components/dashboard/execution-radar.tsx
  - src/components/execution/workspace-shell.tsx
  - src/app/globals.css
  - src/test/execution-workspace.test.tsx
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
must_haves:
  truths:
    - "A mesa de execucao mostra backlog, conversao, detalhe e radar na mesma superficie."
    - "O operador enxerga responsavel, prioridade, status, origem e envelhecimento sem abrir o detalhe."
    - "O detalhe da task preserva a origem vinculada e permite atualizar ownership/progresso sem perder contexto."
    - "Os fluxos principais da UI de execucao sao cobertos por teste dedicado antes de encerrar a fase."
  artifacts:
    - path: "src/components/execution/tasks-table.tsx"
      provides: "Tabela densa de tarefas com colunas operacionais."
    - path: "src/components/execution/conversion-strip.tsx"
      provides: "Faixa de itens prontos para conversao com CTA `Converter em tarefa`."
    - path: "src/components/detail/task-detail-panel.tsx"
      provides: "Painel lateral com ownership, progresso, origem vinculada e historico essencial."
    - path: "src/components/dashboard/execution-radar.tsx"
      provides: "Radar operacional do backlog de execucao."
    - path: "src/test/execution-workspace.test.tsx"
      provides: "Cobertura dos fluxos principais do workspace de execucao."
  key_links:
    - from: "src/components/execution/workspace-shell.tsx"
      to: "src/components/execution/tasks-table.tsx"
      via: "Lista densa e selecao de task"
      pattern: "TasksTable"
    - from: "src/components/execution/workspace-shell.tsx"
      to: "src/components/execution/conversion-strip.tsx"
      via: "Conversao inbox->task"
      pattern: "ConversionStrip"
    - from: "src/components/detail/task-detail-panel.tsx"
      to: "src/app/api/tasks/[id]/status/route.ts"
      via: "Mutacao de progresso"
      pattern: "/api/tasks/.*/status"
---

<objective>
Completar a mesa de execucao da Fase 2 com componentes densos, estilos finais e teste automatizado do workspace.

Purpose: transformar a fundacao de `/execucao` em uma superficie operacional usavel para converter, atribuir, priorizar, atualizar status e consultar origem.
Output: tabela densa de tasks, filtros, faixa `Converter em tarefa`, detalhe de task, radar de execucao, estilos responsivos e teste `execution-workspace`.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-execucao-de-trabalho/02-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@.planning/phases/02-execucao-de-trabalho/02-03-PLAN.md
@.planning/phases/01-base-operacional/01-base-operacional-03-SUMMARY.md

<interfaces>
Required UI copy from UI-SPEC:
- `Converter em tarefa`
- `Itens prontos para conversao`
- `Responsavel`
- `Status da tarefa`
- `Origem vinculada`
- `Backlog de execucao`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar componentes densos e integrar ao shell de execucao</name>
  <files>src/components/execution/tasks-table.tsx, src/components/execution/filters-bar.tsx, src/components/execution/conversion-strip.tsx, src/components/detail/task-detail-panel.tsx, src/components/dashboard/execution-radar.tsx, src/components/execution/workspace-shell.tsx</files>
  <read_first>src/components/inbox/inbox-table.tsx, src/components/inbox/filters-bar.tsx, src/components/detail/item-detail-panel.tsx, src/components/dashboard/ops-radar.tsx, src/components/inbox/new-entry-dialog.tsx, src/components/execution/workspace-shell.tsx, .planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md, .planning/phases/02-execucao-de-trabalho/02-PATTERNS.md</read_first>
  <action>Implemente `src/components/execution/tasks-table.tsx` usando o mesmo padrao da inbox table com `@tanstack/react-table`, linhas clicaveis e colunas densas nesta ordem: `Tarefa`, `Responsavel`, `Prioridade`, `Status`, `Origem`, `Envelhecimento`. Em `src/components/execution/filters-bar.tsx`, mantenha `select` controlado e labels visiveis, mas inclua filtros por `status`, `priority`, `owner`, `ageBucket` e `sort`. Em `src/components/execution/conversion-strip.tsx`, renderize a faixa `Itens prontos para conversao` acima da lista principal; cada item deve mostrar origem, resumo curto, proxima acao revisada e CTA `Converter em tarefa`, com prefill de `priority` a partir do inbox e `status` inicial `Nao iniciada`, mas `ownerName` vazio para decisao humana. Em `src/components/detail/task-detail-panel.tsx`, siga a mesma casca do `item-detail-panel`, trocando o corpo por campos editaveis de `Responsavel`, `Prioridade`, `Status da tarefa`, `Ultima movimentacao`, bloco `Origem vinculada` e timeline minima. Em `src/components/dashboard/execution-radar.tsx`, limite o resumo a cinco blocos: `Prontos para conversao`, `Sem responsavel`, `Bloqueadas`, `Criticas`, `Envelhecidas`. Integre esses componentes em `workspace-shell.tsx` usando os handlers criados no plano 03. Nao use kanban, drag-and-drop ou menus profundos.</action>
  <acceptance_criteria>`rg "Converter em tarefa|Itens prontos para conversao|Responsavel|Status da tarefa|Origem vinculada|Sem responsavel|Bloqueadas|Envelhecidas" src/components/execution/conversion-strip.tsx src/components/detail/task-detail-panel.tsx src/components/dashboard/execution-radar.tsx` confirma os blocos obrigatorios; `rg "createColumnHelper|owner|ageBucket|priority|status" src/components/execution/tasks-table.tsx src/components/execution/filters-bar.tsx` confirma a superficie operacional densa; `rg "TasksTable|ConversionStrip|TaskDetailPanel|ExecutionRadar" src/components/execution/workspace-shell.tsx` confirma integracao.</acceptance_criteria>
  <verify>
    <automated>rg "Converter em tarefa|Itens prontos para conversao|Responsavel|Status da tarefa|Origem vinculada|Sem responsavel|Bloqueadas|Envelhecidas" src/components/execution/conversion-strip.tsx src/components/detail/task-detail-panel.tsx src/components/dashboard/execution-radar.tsx && rg "createColumnHelper|owner|ageBucket|priority|status" src/components/execution/tasks-table.tsx src/components/execution/filters-bar.tsx && rg "TasksTable|ConversionStrip|TaskDetailPanel|ExecutionRadar" src/components/execution/workspace-shell.tsx</automated>
  </verify>
  <done>A mesa de execucao mostra backlog, conversao, detalhe e radar com a hierarquia visual aprovada e sem esconder o vinculo entre tarefa e item original.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar estilos globais e cobrir os fluxos principais com testes de workspace</name>
  <files>src/app/globals.css, src/test/execution-workspace.test.tsx, src/components/execution/workspace-shell.tsx</files>
  <read_first>src/app/globals.css, src/test/inbox-workspace.test.tsx, src/test/triage-review-ui.test.tsx, .planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md, .planning/phases/02-execucao-de-trabalho/02-PATTERNS.md</read_first>
  <action>Estenda `src/app/globals.css` sem quebrar a Fase 1, adicionando classes para `execution-*`, chips de status de task, faixa de conversao, card de origem vinculada, navegacao lateral compartilhada e responsividade mobile prevista no UI-SPEC. Preserve as tokens existentes (`--background`, `--surface`, `--accent`, IBM Plex Sans, spacing de 4px) e use `#D9E2D3` para sidebar/faixa de conversao, `#C65D2E` apenas para CTA, foco e estado ativo. Em `src/test/execution-workspace.test.tsx`, cubra ao menos: renderizacao server-hydrated da lista de tasks, filtro por status/owner, selecao de task, conversao via faixa `Converter em tarefa`, update de `ownerName`/`priority`, update de status e refresh do radar. Use mocks de `fetch` no mesmo estilo de `src/test/inbox-workspace.test.tsx` e mantenha mensagens de erro/notificacao consistentes com a copy do UI-SPEC.</action>
  <acceptance_criteria>`rg "workspace-sidebar__nav|conversion-strip|task-detail|execution-radar|#c65d2e|#d9e2d3" src/app/globals.css` confirma os estilos centrais; `rg "Converter em tarefa|Filtrar por status|Responsavel|Nao foi possivel carregar o backlog de execucao" src/test/execution-workspace.test.tsx` confirma os fluxos automatizados; `npm run test -- --run src/test/execution-workspace.test.tsx` passa.</acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/test/execution-workspace.test.tsx && npm run lint && npm run build</automated>
  </verify>
  <done>A Fase 2 possui UI operacional completa, consistente com o design aprovado e com testes cobrindo as interacoes principais do workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client components->API tasks | conversao, ownership e status sao enviados pelo browser e precisam ser tratados como input nao confiavel |
| task detail->origin display | o card de origem deve vir do dado persistido, nao de campos editaveis do operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | I | `src/components/detail/task-detail-panel.tsx` | mitigate | renderizar `Origem vinculada` a partir de `TaskDetail.origin`, nunca de campos editaveis pelo operador |
| T-02-11 | D | `src/components/execution/conversion-strip.tsx` | mitigate | desabilitar CTA enquanto `isPending` e refrescar `readyToConvert` apos conversao para evitar cliques repetidos |
| T-02-13 | T | `src/components/execution/workspace-shell.tsx` | mitigate | apos cada mutacao, refazer leitura de `/api/tasks` e substituir estado otimista pela resposta server-side |
</threat_model>

<verification>
Executar `npm run test -- --run src/test/execution-workspace.test.tsx`, `npm run lint` e `npm run build`. Confirmar por leitura da UI implementada que: a navegacao lateral mostra `Inbox` e `Tarefas`; a faixa `Itens prontos para conversao` aparece acima da lista; a lista densa expoe `Responsavel`, `Prioridade`, `Status`, `Origem`, `Envelhecimento`; o detalhe preserva `Origem vinculada`.
</verification>

<success_criteria>
- `/execucao` vira a superficie principal da Fase 2 com backlog, conversao e detalhe no mesmo fluxo.
- A operacao distingue explicitamente backlog de entrada de backlog de execucao.
- O operador consegue converter, atribuir, priorizar, atualizar status e consultar a origem da tarefa sem sair da mesa de execucao.
- `src/test/execution-workspace.test.tsx`, `npm run lint` e `npm run build` passam ao final do plano.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execucao-de-trabalho/02-execucao-de-trabalho-04-SUMMARY.md`
</output>
