---
phase: 02-execucao-de-trabalho
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/app/(workspace)/execucao/page.tsx
  - src/app/(workspace)/execucao/loading.tsx
  - src/app/(workspace)/execucao/error.tsx
  - src/components/execution/workspace-shell.tsx
  - src/components/inbox/workspace-shell.tsx
autonomous: true
requirements:
  - TASK-01
  - TASK-04
must_haves:
  truths:
    - "A rota `/execucao` existe como entrada server-first do backlog de execucao."
    - "A navegacao lateral diferencia explicitamente `Inbox` e `Tarefas` sem quebrar o workspace atual."
    - "O shell de execucao recebe dados reais da API de tasks e preserva o vinculo com origem para o plano seguinte."
    - "Este plano cria somente a fundacao UI; componentes densos, estilos finais e testes ficam no plano 04."
  artifacts:
    - path: "src/app/(workspace)/execucao/page.tsx"
      provides: "Entrada server-first com items, selectedItem, summary e readyToConvert."
    - path: "src/components/execution/workspace-shell.tsx"
      provides: "Shell cliente inicial com estado, refresh centralizado e slots para a mesa de execucao."
    - path: "src/components/inbox/workspace-shell.tsx"
      provides: "Navegacao compartilhada Inbox/Tarefas."
  key_links:
    - from: "src/app/(workspace)/execucao/page.tsx"
      to: "src/db/queries/tasks.ts"
      via: "Leitura server-first do workspace de execucao"
      pattern: "getTasks|getTaskById|getExecutionSummary|getReadyToConvertItems"
    - from: "src/app/(workspace)/execucao/page.tsx"
      to: "src/components/execution/workspace-shell.tsx"
      via: "Hydration server-first"
      pattern: "initialItems|initialSummary|initialReadyToConvert"
    - from: "src/components/inbox/workspace-shell.tsx"
      to: "src/app/(workspace)/execucao/page.tsx"
      via: "Navegacao lateral"
      pattern: "href=\"/execucao\""
---

<objective>
Criar a fundacao da mesa de execucao em `/execucao`, com rota server-first, estados de loading/error e navegacao Inbox/Tarefas, sem ainda implementar a superficie densa de componentes.

Purpose: abrir o caminho de execucao rastreavel depois da triagem, mantendo a separacao entre backlog de entrada e backlog de execucao.
Output: rota `/execucao`, shell cliente inicial, loading/error states e navegacao compartilhada prontos para receber os componentes do plano 04.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-execucao-de-trabalho/02-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@.planning/phases/02-execucao-de-trabalho/02-02-PLAN.md
@.planning/phases/01-base-operacional/01-base-operacional-03-SUMMARY.md

<interfaces>
Expected execution shell inputs:
```ts
type ExecutionWorkspaceProps = {
  initialItems: TaskListItem[];
  initialSelectedItem: TaskDetail | null;
  initialSummary: TaskSummary;
  initialReadyToConvert: ReadyToConvertItem[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar a rota `/execucao` server-first e estados da rota</name>
  <files>src/app/(workspace)/execucao/page.tsx, src/app/(workspace)/execucao/loading.tsx, src/app/(workspace)/execucao/error.tsx</files>
  <read_first>src/app/(workspace)/inbox/page.tsx, src/app/(workspace)/inbox/loading.tsx, src/app/(workspace)/inbox/error.tsx, src/db/queries/tasks.ts, src/types/tasks.ts, .planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md, .planning/phases/02-execucao-de-trabalho/02-PATTERNS.md</read_first>
  <action>Crie `src/app/(workspace)/execucao/page.tsx` no mesmo desenho server-first da pagina de inbox: carregar `items`, `selectedItem`, `summary` e `readyToConvert` via exports de `src/db/queries/tasks.ts` (`getTasks`, `getTaskById`, `getExecutionSummary`, `getReadyToConvertItems`) antes da renderizacao e repassar tudo ao shell cliente. Nao faca self-fetch de `/api/tasks` na Server Component; a API fica reservada para refresh e mutacoes no shell cliente. Quando nao houver item selecionado, use o primeiro item retornado pelas queries como selecao inicial. Adicione `loading.tsx` e `error.tsx` analogos aos da Fase 1, mas com copy de execucao: backlog de execucao, conversao e vinculo de origem.</action>
  <acceptance_criteria>`rg "getTasks|getTaskById|getExecutionSummary|getReadyToConvertItems|initialReadyToConvert|initialSummary|initialSelectedItem" src/app/(workspace)/execucao/page.tsx` confirma a rota e os dados iniciais via queries; `rg "Backlog de execucao|Converter em tarefa|Nao foi possivel carregar o backlog de execucao" src/app/(workspace)/execucao/error.tsx src/app/(workspace)/execucao/loading.tsx` confirma estados da rota.</acceptance_criteria>
  <verify>
    <automated>rg "getTasks|getTaskById|getExecutionSummary|getReadyToConvertItems|initialReadyToConvert|initialSummary|initialSelectedItem" src/app/(workspace)/execucao/page.tsx && rg "Backlog de execucao|Converter em tarefa|Nao foi possivel carregar o backlog de execucao" src/app/(workspace)/execucao/error.tsx src/app/(workspace)/execucao/loading.tsx</automated>
  </verify>
  <done>A rota `/execucao` renderiza com dados iniciais reais e estados de loading/error coerentes com a linguagem da Fase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Criar shell inicial e navegacao compartilhada Inbox/Tarefas</name>
  <files>src/components/execution/workspace-shell.tsx, src/components/inbox/workspace-shell.tsx</files>
  <read_first>src/components/inbox/workspace-shell.tsx, src/app/globals.css, src/app/layout.tsx, src/app/api/tasks/[id]/route.ts, src/app/api/inbox/[id]/convert/route.ts, src/app/api/tasks/[id]/status/route.ts, .planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md, .planning/phases/02-execucao-de-trabalho/02-PATTERNS.md</read_first>
  <action>Crie `src/components/execution/workspace-shell.tsx` como Client Component com `selectedId`, `selectedItem`, `summary`, `readyToConvert`, `notice`, `errorMessage` e `useTransition`. O shell deve expor helpers de refresh e mutacao consumindo `/api/tasks`, `/api/tasks/[id]`, `/api/inbox/[id]/convert` e `/api/tasks/[id]/status`, mas pode renderizar placeholders estruturais para tabela, faixa e detalhe ate o plano 04 criar os componentes finais. Atualize `src/components/inbox/workspace-shell.tsx` para incluir navegacao lateral com links `Inbox` -> `/inbox` e `Tarefas` -> `/execucao`, destacando a area ativa com o accent do design system. O label visual continua `Tarefas`, mas a rota de implementacao desta fase e `/execucao` per pattern map.</action>
  <acceptance_criteria>`rg "useTransition|readyToConvert|refreshWorkspace|/api/tasks|/api/inbox/.*/convert|/api/tasks/.*/status" src/components/execution/workspace-shell.tsx` confirma estado e endpoints; `rg "workspace-sidebar__nav|href=\\\"/inbox\\\"|href=\\\"/execucao\\\"|Tarefas" src/components/inbox/workspace-shell.tsx` confirma navegacao.</acceptance_criteria>
  <verify>
    <automated>rg "useTransition|readyToConvert|refreshWorkspace|/api/tasks|/api/inbox/.*/convert|/api/tasks/.*/status" src/components/execution/workspace-shell.tsx && rg "workspace-sidebar__nav|href=\\\"/inbox\\\"|href=\\\"/execucao\\\"|Tarefas" src/components/inbox/workspace-shell.tsx</automated>
  </verify>
  <done>O shell de execucao e a navegacao compartilhada existem sem depender de teste ou componentes que so serao criados no plano 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page->client shell | dados iniciais do workspace cruzam para o cliente e devem continuar tipados/coerentes apos refresh |
| client shell->API tasks | acoes de conversao e update partem do browser e precisam manter validacao no servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | `src/components/execution/workspace-shell.tsx` | mitigate | usar apenas endpoints server-side para conversao e updates; nao mutar estado local como fonte final de verdade |
| T-02-12 | S | `src/components/inbox/workspace-shell.tsx` | accept | sem auth nesta milestone, o risco de spoofing de ownership e baixo e limitado pela validacao server-side dos payloads |
</threat_model>

<verification>
Executar os comandos `rg` definidos nas tarefas e confirmar por leitura que: a rota `/execucao` carrega `items`, `selectedItem`, `summary` e `readyToConvert`; a navegacao lateral mostra `Inbox` e `Tarefas`; o shell inicial nao importa componentes que pertencem ao plano 04 antes de eles existirem.
</verification>

<success_criteria>
- `/execucao` existe como superficie server-first da Fase 2.
- A operacao consegue navegar claramente entre backlog de entrada e backlog de execucao.
- O shell inicial tem contratos e handlers prontos para a superficie densa do plano 04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execucao-de-trabalho/02-execucao-de-trabalho-03-SUMMARY.md`
</output>
