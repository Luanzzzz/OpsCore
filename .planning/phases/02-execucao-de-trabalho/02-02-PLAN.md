---
phase: 02-execucao-de-trabalho
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/inbox/[id]/convert/route.ts
  - src/app/api/tasks/route.ts
  - src/app/api/tasks/[id]/route.ts
  - src/app/api/tasks/[id]/status/route.ts
  - src/test/tasks-api.test.ts
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
must_haves:
  truths:
    - "O operador consegue converter um item de inbox revisado em tarefa via API dedicada."
    - "A lista de tarefas devolve backlog, resumo de execucao e faixa de itens prontos para conversao na mesma leitura."
    - "Cada tarefa possui detalhe consultavel e mutacoes separadas para metadata e progresso."
    - "IDs invalidos, origens ausentes e conversoes duplicadas retornam 400/404/409 de forma deterministica."
  artifacts:
    - path: "src/app/api/inbox/[id]/convert/route.ts"
      provides: "Mutacao server-side de conversao inbox->task."
    - path: "src/app/api/tasks/route.ts"
      provides: "Leitura do workspace de execucao com `items`, `summary` e `readyToConvert`."
    - path: "src/app/api/tasks/[id]/status/route.ts"
      provides: "Mutacao explicita de progresso operacional."
  key_links:
    - from: "src/app/api/inbox/[id]/convert/route.ts"
      to: "src/db/queries/tasks.ts"
      via: "Chamada de `createTaskFromInbox` com traducao de conflitos para HTTP"
      pattern: "createTaskFromInbox"
    - from: "src/app/api/tasks/route.ts"
      to: "src/db/queries/tasks.ts"
      via: "Agregacao do workspace de execucao"
      pattern: "getTasks|getExecutionSummary|getReadyToConvertItems"
    - from: "src/app/api/tasks/[id]/route.ts"
      to: "src/lib/validation/tasks.ts"
      via: "Validacao de payload PATCH"
      pattern: "updateTaskMetaSchema"
---

<objective>
Expor a superficie HTTP da Fase 2 com rotas dedicadas para conversao, listagem do backlog de execucao, detalhe e atualizacao de ownership/progresso.

Purpose: manter todas as regras criticas de conversao e update no servidor, evitando logica fragmentada no cliente e preservando status HTTP verificaveis.
Output: route handlers App Router para execucao de trabalho e testes de API cobrindo fluxo feliz, payload invalido, not found e conflito.
</objective>

<execution_context>
@C:/Users/Luan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Luan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-execucao-de-trabalho/02-RESEARCH.md
@.planning/phases/02-execucao-de-trabalho/02-UI-SPEC.md
@.planning/phases/02-execucao-de-trabalho/02-PATTERNS.md
@.planning/phases/02-execucao-de-trabalho/02-01-PLAN.md
@.planning/phases/01-base-operacional/01-base-operacional-04-SUMMARY.md

<interfaces>
Expected from src/types/tasks.ts:
```ts
export interface CreateTaskFromInboxInput {
  originInboxId: number;
  title: string;
  ownerName?: string | null;
  priority: TaskPriority;
  status: TaskStatus;
  contextNote?: string | null;
}

export interface UpdateTaskMetaInput {
  ownerName?: string | null;
  priority?: TaskPriority;
}

export interface UpdateTaskStatusInput {
  status: TaskStatus;
  movementNote?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar leitura do workspace e conversao inbox->task por rotas App Router</name>
  <files>src/app/api/inbox/[id]/convert/route.ts, src/app/api/tasks/route.ts, src/app/api/tasks/[id]/route.ts, src/test/tasks-api.test.ts</files>
  <read_first>src/app/api/inbox/route.ts, src/app/api/inbox/[id]/route.ts, src/db/queries/tasks.ts, src/lib/validation/tasks.ts, src/types/tasks.ts, .planning/phases/02-execucao-de-trabalho/02-PATTERNS.md</read_first>
  <behavior>
    - Test 1: `POST /api/inbox/[id]/convert` com origem valida cria tarefa e retorna `201`.
    - Test 2: `POST /api/inbox/[id]/convert` retorna `409` quando o inbox ja possui tarefa ativa.
    - Test 3: `GET /api/tasks` retorna `{ items, summary, readyToConvert }` com filtros `status`, `priority`, `owner`, `sort`, e cada item pronto para conversao inclui `reviewedCategory` e `reviewedNextAction`.
    - Test 4: `GET /api/tasks/[id]` retorna `TaskDetail` completo e `404` para tarefa inexistente.
  </behavior>
  <action>Crie `src/app/api/inbox/[id]/convert/route.ts` seguindo o padrao de `src/app/api/inbox/[id]/review/route.ts`: parse do `id`, `400` para id invalido, payload validado com `createTaskFromInboxSchema` e resposta `201` com a tarefa criada. Use `originInboxId` do corpo como espelho do path apenas para consistencia; rejeite divergencia entre path e body com `400`. Em `src/app/api/tasks/route.ts`, implemente `GET` com `taskFiltersSchema`, retornando JSON no formato `{ items, summary, readyToConvert }` para a mesa de execucao; preserve no `readyToConvert` o shape enriquecido vindo do dominio, incluindo `reviewedCategory`, `reviewedNextAction`, `priorityReviewed` e `summaryShort`. Em `src/app/api/tasks/[id]/route.ts`, implemente `GET` para detalhe e `PATCH` exclusivamente para `ownerName` e `priority` usando `updateTaskMetaSchema`. Mantenha nomes de erro objetivos: `Invalid task id`, `Task not found`, `Inbox item not found`, `Inbox item already has an active task`.</action>
  <acceptance_criteria>`rg "export async function POST|export async function GET|export async function PATCH" src/app/api/inbox/[id]/convert/route.ts src/app/api/tasks/route.ts src/app/api/tasks/[id]/route.ts` encontra os handlers; `rg "readyToConvert|summary|reviewedNextAction|reviewedCategory|priorityReviewed|Inbox item already has an active task" src/app/api/tasks/route.ts src/app/api/inbox/[id]/convert/route.ts src/test/tasks-api.test.ts` confirma payload e conflitos; `npm run test -- --run src/test/tasks-api.test.ts` passa.</acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/test/tasks-api.test.ts</automated>
  </verify>
  <done>As rotas de conversao, listagem e detalhe da execucao funcionam com filtros tipados e conflitos de dominio traduzidos para HTTP estavel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor mutacao de progresso operacional sem misturar revisao de triagem com execucao</name>
  <files>src/app/api/tasks/[id]/status/route.ts, src/test/tasks-api.test.ts</files>
  <read_first>src/app/api/inbox/[id]/review/route.ts, src/app/api/tasks/[id]/route.ts, src/db/queries/tasks.ts, src/lib/validation/tasks.ts, .planning/phases/02-execucao-de-trabalho/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `POST /api/tasks/[id]/status` atualiza `status`, `lastMovementAt` e `timeline`.
    - Test 2: a rota aceita exatamente `Nao iniciada`, `Em andamento`, `Bloqueada`, `Concluida` e rejeita qualquer outro valor com `400`.
    - Test 3: `POST /api/tasks/[id]/status` devolve `404` para tarefa inexistente.
    - Test 4: `PATCH /api/tasks/[id]` e `POST /api/tasks/[id]/status` podem ser usados em sequencia sem apagar `ownerName`, `priority` ou `origin`.
  </behavior>
  <action>Implemente `src/app/api/tasks/[id]/status/route.ts` como mutacao dedicada de progresso operacional, separada de `PATCH /api/tasks/[id]`. O handler deve parsear o `id`, validar o corpo com `updateTaskStatusSchema`, chamar `updateTaskStatus` e devolver a tarefa atualizada. Preservar essa separacao e obrigatorio para TASK-03: nao mova status de task para a rota de revisao do inbox nem para um schema compartilhado com triagem. Amplie `src/test/tasks-api.test.ts` com cenarios que chamam os handlers diretamente, no mesmo estilo dos testes de inbox existentes, cobrindo fluxo feliz, enum invalido, not found e persistencia do vinculo com a origem depois das mutacoes.</action>
  <acceptance_criteria>`rg "updateTaskStatusSchema|Task not found|status_updated" src/app/api/tasks/[id]/status/route.ts src/test/tasks-api.test.ts` confirma a mutacao dedicada; `npm run test -- --run src/test/tasks-api.test.ts` passa com cenarios de `201/200`, `400`, `404` e `409` para a superficie completa da fase.</acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/test/tasks-api.test.ts</automated>
  </verify>
  <done>Ownership, prioridade e progresso da task possuem endpoints distintos, validacao server-side e cobertura automatizada suficiente para sustentar a UI da Fase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API tasks | payload do operador cruza a fronteira e pode tentar forcar ids ou enums invalidos |
| API tasks→queries tasks | handlers traduzem entrada HTTP em mutacoes de dominio e precisam preservar erros sem vazamento indevido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | `src/app/api/inbox/[id]/convert/route.ts` | mitigate | validar alinhamento entre `params.id` e `originInboxId`, e mapear conflito de conversao para `409` |
| T-02-06 | T | `src/app/api/tasks/[id]/status/route.ts` | mitigate | aceitar apenas enums definidos em `updateTaskStatusSchema` e rejeitar payloads arbitrarios com `400` |
| T-02-07 | I | `src/app/api/tasks/route.ts` | mitigate | devolver somente `items`, `summary` e `readyToConvert`; nao expor campos internos do store ou erros brutos |
| T-02-08 | R | `src/app/api/tasks/[id]/route.ts` | mitigate | sempre retornar `TaskDetail` vindo do dominio persistido, preservando `origin` e `timeline` apos PATCH/POST |
</threat_model>

<verification>
Executar `npm run test -- --run src/test/tasks-api.test.ts` e confirmar via leitura do código que: `POST /api/inbox/[id]/convert` usa `createTaskFromInboxSchema`; `GET /api/tasks` retorna `items`, `summary` e `readyToConvert`; `PATCH /api/tasks/[id]` nao altera status; `POST /api/tasks/[id]/status` nao altera `ownerName`/`priority`.
</verification>

<success_criteria>
- A superficie HTTP da execucao cobre conversao, backlog, detalhe, ownership, prioridade e status.
- Os handlers retornam 400/404/409 de forma previsivel para os cenarios de erro relevantes.
- A UI da fase pode depender exclusivamente dessa API sem reimplementar regras de dominio no cliente.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execucao-de-trabalho/02-execucao-de-trabalho-02-SUMMARY.md`
</output>
